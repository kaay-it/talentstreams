= TalentStreams — Анализ и бэклог доработок
:toc: left
:toc-title: Содержание
:toclevels: 3
:sectnums:
:icons: font
:lang: ru

== Что реализовано

[cols="1,3", options="header"]
|===
| Компонент | Статус

| Главная страница `/`
| Реализована: hero, описание механики, формы регистрации работодателя и кандидата

| Страница подборки `/list/[listId]`
| Реализована: анонимные карточки кандидатов (без имени и контактов), теги, summary, disclaimer о конфиденциальности. Кнопка «Хочу связаться» — функциональна: записывает запрос в лист «Contact Requests». Блок «Не нашли подходящих кандидатов?» — виден только при наличии токена работодателя (`?e=`). Noindex. Доступ только по `?e=[employer_token]` (персонализированная подборка) или `?secret=[EDITOR_SECRET]` (редактор); без параметров — 404.

| Страница профиля `/profile/[id]`
| Реализована: полный профиль кандидата. Недоступна с публичных страниц. Noindex.

| Редактор `/editor`
| Реализован: боковое меню с разделами (Выпуски / Работодатели / Запросы / Настройки). Выпуски: история кампаний, проверка пустой книги, ссылка на подборку с секретом. Работодатели: три коллапсируемые группы (На проверке / Отклонён / Подтверждён). Запросы: управление запросами на контакт с двумя разделами («Общие запросы» и «По кандидатам»), цветные бейджи статусов, смена статуса через выпадающий список. Настройки: миграция колонок Google Sheets.

| SendPulse интеграция
| Реализована: адресные книги (авто-создание), email-кампании, пагинация, TTL кэша книг (60 с). Проверка пустой книги до запуска рассылки.

| API `/api/publish/[listId]`
| Реализован: curl-triggered публикация с авторизацией по токену

| Регистрация работодателя
| Реализована: форма → Google Sheets со статусом «На проверке». В SendPulse не добавляется — только после подтверждения редактором.

| Подтверждение работодателя
| Реализовано: в `/editor` редактор подтверждает или отклоняет заявку. При подтверждении — добавление в SendPulse (мастер-книга + книги выбранных стримов).

| Регистрация кандидата
| Реализована: форма → Google Sheets (SendPulse не используется — кандидаты не являются подписчиками рассылки)

| Google Sheets интеграция
| Реализована: чтение профилей, подборок, стримов, работодателей; запись регистраций; миграция колонок (`ensureProfileColumns`)

| Robots / noindex
| Реализовано: `app/robots.ts` (Disallow: /list/, /profile/), meta robots noindex на страницах подборки и профиля

| Персонализация рассылок
| Реализована: токен работодателя (`crypto.randomUUID()`, колонка `ID` в Employers); `employer_token` передаётся в SendPulse; ссылка в письме `/list/[listId]?e={{employer_token}}`; `getEmployerByToken()` и `filterCandidatesForEmployer()` в `lib/sheets.ts`; поля `excludedCompanies`/`excludedIndustries` в `Profile`.

| Запросы на контакт (Contact Requests)
| Реализованы: лист «Contact Requests» в Google Sheets автосоздаётся при первом обращении. Два типа запросов: по кандидату (`candidateId` заполнен, 7 статусов) и общий (`candidateId` пуст, 3 статуса — «Новый запрос» / «Взяли в работу» / «Завершён»). Поля строки: ID (UUID), Timestamp, List ID, Stream, Candidate ID, Employer Token, Employer Name, Company, Employer Email, Status.
|===

== Выполненные задачи

=== ✅ TASK-04: Пять структурированных тегов кандидата

*Выполнено:*

* Добавлены поля `level`, `industry`, `func`, `countryPrimary`, `countryDesired` в тип `Profile` (`lib/sheets.ts`).
* Добавлены алиасы заголовков на английском и русском в `HEADER_ALIASES` (`Level`/`Уровень`, `Industry`/`Отрасль`, `Function`/`Функция`, `Country Primary`/`Страна текущая`/`Основная страна`, `Country Desired`/`Страна желаемая`/`Желаемая страна`).
* Поля читаются из Google Sheets в `buildProfile`.
* Добавлена функция `ensureProfileColumns()` (`lib/sheets.ts`) — проверяет наличие колонок в таблице и дописывает недостающие.
* Добавлена кнопка «Проверить или добавить колонки в таблицу» в редакторе (`components/add-columns-button.tsx`).

---

=== ✅ TASK-03: Анонимизация кандидатов на странице подборки

*Выполнено:*

* Карточка кандидата в `/list/[listId]/page.tsx` переработана: убраны имя, ссылка на полный профиль и прямые контакты.
* Показываются только анонимные данные: должность (`title`), страна (`countryPrimary` + `countryDesired`), теги (`level`, `industry`, `func`) и текст `summary`.
* Поле `summary` добавлено в тип `Profile`, алиасы `HEADER_ALIASES`, `buildProfile` и в миграцию колонок (`ensureProfileColumns`).
* Профиль кандидата перенесён с `/[id]` на `/profile/[id]`; со страницы подборки прямых ссылок нет.
* Добавлен мета-тег `robots: noindex, nofollow` на страницах `/list/[listId]` и `/profile/[id]`.
* Добавлен `app/robots.ts` с `Disallow: /list/, /profile/`.
* Добавлена кнопка-заглушка «Хочу связаться» (`components/contact-button.tsx`) — готова к реализации в TASK-01.

---

=== ✅ TASK-09: Процесс подтверждения регистрации работодателя

*Выполнено:*

* `Status` добавлен в `EMPLOYERS_HEADERS`; `registerEmployer` пишет «На проверке» и не добавляет в SendPulse.
* Добавлены `getEmployers()` и `updateEmployerStatus(rowIndex, status)` в `lib/sheets.ts`.
* Добавлены Server Actions `confirmEmployer()` (сначала SendPulse, затем статус) и `rejectEmployer()` с `revalidatePath("/editor")`.
* Компонент `EmployerSection` в редакторе: три коллапсируемые группы — «На проверке» (открыта), «Отклонён» и «Подтверждён» (свёрнуты). Ошибки показываются inline под строкой работодателя.

---

=== ✅ TASK-13: Персонализация рассылок — токен работодателя и предпочтения кандидата

*Выполнено:*

*Часть 1 — токен работодателя:*

* Колонка `ID` добавлена в лист Employers; `registerEmployer` генерирует `crypto.randomUUID()` (хранится как `ID` в Sheets, используется как токен работодателя).
* `appendEmployerRow` читает реальные заголовки листа перед записью (header-mapped write, не позиционный) — работает при любом порядке колонок.
* `confirmEmployer` передаёт переменную `employer_token` в контакт SendPulse.
* URL шаблона письма обновлён: `/list/[listId]?e={{employer_token}}`.
* Добавлен `getEmployerByToken(token)` в `lib/sheets.ts`.
* Тип `Employer` теперь содержит поле `token: string`.

*Часть 2 — предпочтения кандидата:*

* Тип `Profile` теперь содержит поля `excludedCompanies: string[]` и `excludedIndustries: string[]`.
* `HEADER_ALIASES` дополнен алиасами: `"excluded companies"` → `excludedCompanies`, `"excluded industries"` → `excludedIndustries` (и русские варианты).
* `DISTRIBUTION_COLUMNS` обновлён: добавлены `"Excluded Companies"` и `"Excluded Industries"` — кнопка «Настройки» добавит их в таблицу.
* Добавлена `filterCandidatesForEmployer(candidates, employer)` в `lib/sheets.ts`.

*Часть 3 — персонализированная страница подборки:*

* `/list/[listId]` теперь требует `?e=[token]` или `?secret=[EDITOR_SECRET]`.
* `?e=[token]`: работодатель находится через `getEmployerByToken`, кандидаты фильтруются через `filterCandidatesForEmployer`; невалидный/отсутствующий токен → 404.
* `?secret=[EDITOR_SECRET]`: все кандидаты показываются без фильтрации.
* Без параметров → `notFound()` (404).
* `employerToken` передаётся в `ContactButton` (готово для TASK-01).

*Дополнительно:* регистрация теперь проверяет уникальность email и телефона (среди статусов «На проверке» и «Подтверждён»), телефон сравнивается без нецифровых символов.

---

=== ✅ TASK-01: Кнопка «Хочу связаться» на странице подборки

*Выполнено:*

* `appendContactRequest()` добавлена в `lib/sheets.ts` — записывает строку в лист «Contact Requests» с полями: ID (UUID), Timestamp, List ID, Stream, Candidate ID, Employer Token, Employer Name, Company, Employer Email, Status («Новый запрос»). Лист создаётся автоматически при первом обращении через `ensureSheet`.
* Server Action `submitContactRequest(candidateId, listId, employerToken)` добавлен в `app/actions.ts` — находит работодателя по токену через `getEmployerByToken`, получает стрим через `getMailingLists`, вызывает `appendContactRequest`.
* `ContactButton` (`components/contact-button.tsx`) полностью функциональна: вызывает Server Action, показывает «Отправка…» в процессе, подтверждение об успехе, inline-ошибку с иконкой AlertCircle. При отсутствии `employerToken` (вид редактора): кнопка неактивна, метка «работодатель не определён».

---

=== ✅ TASK-02: Интерфейс управления запросами в редакторе

*Выполнено:*

* Тип `ContactRequest` и union-тип `ContactRequestStatus` добавлены в `lib/sheets.ts`.
* `getContactRequests()` — читает лист «Contact Requests» с динамическим маппингом колонок по заголовкам.
* `updateContactRequestStatus(rowIndex, status)` — читает заголовочную строку для поиска колонки Status, обновляет конкретную ячейку.
* Server Action `setContactRequestStatus(rowIndex, status)` добавлен в `app/actions.ts` с `revalidatePath("/editor")`.
* Страница `app/editor/requests/page.tsx` — загружает запросы и профили параллельно, показывает бейдж «N новых».
* `components/contact-requests-section.tsx` — два раздела:
  ** «Общие запросы»: запросы без `candidateId` — 3 статуса (Новый запрос / Взяли в работу / Завершён).
  ** «По кандидатам»: запросы с `candidateId` — 7 статусов (Новый запрос, Кандидату написали, Кандидат не отвечает, Связаться с Аленой, Кандидату интересно, Кандидату не интересно, Контакты переданы).
  ** Цветные бейджи статусов; `<select>` выпадающий список меняет статус через `useTransition`.
* «Запросы» добавлен в `EditorNav` (иконка Handshake, между Работодатели и Настройки).

---

=== ✅ TASK-08: Блок «Не нашли подходящих кандидатов?»

*Выполнено (реализация отличается от исходного ТЗ):*

* Блок добавлен в нижнюю часть `/list/[listId]` — виден только при наличии `employerToken` (не отображается при доступе редактора через `?secret=`).
* `GeneralInquiryButton` (`components/general-inquiry-button.tsx`) вызывает `submitGeneralInquiry(listId, employerToken)`.
* `submitGeneralInquiry` пишет в лист «Contact Requests» с `candidateId: ""` (пустой) — отдельная форма не нужна, т. к. данные работодателя уже известны из токена.
* Отправка на email администратора *не реализована* — запросы хранятся в «Contact Requests» и отображаются в редакторе в разделе «Общие запросы» с 3 статусами.
* `ContactRequestStatus` расширен статусами «Взяли в работу» и «Завершён».

---

== Задачи на доработку

=== 🟠 Важно — бизнес-правила

==== TASK-05: Правила публикации кандидатов

*Что сейчас:* нет отслеживания дат публикации, нет защиты от нарушения правил.

*По ТЗ (§13):*

* Первая публикация → фиксируется дата.
* Через 2 недели возможна повторная публикация.
* После второй публикации — пауза 3 месяца.
* Кандидат может попросить статус «Не публиковать».

*Что нужно сделать:*

* Добавить в карточку кандидата (Google Sheets): дата первой публикации, дата последней публикации, статус (Активный / На паузе до даты / Не публиковать).
* В редакторе при выборе кандидатов для выпуска: показывать статус публикации и дату, с которой публикация снова разрешена.
* Предупреждение, если кандидат добавлен в подборку, но находится в паузе.

---

==== TASK-06: Два обязательных канала доставки для работодателя

*Что сейчас:* форма работодателя имеет `primaryContact` (один канал).

*По ТЗ (§10):* работодатель обязан выбрать основной и резервный канал (Telegram, WhatsApp, Email).

*Что нужно сделать:*

* Добавить поле `backupContact` в форму работодателя и в `EmployerData`.
* Сохранять оба канала в Google Sheets.
* Валидация: оба канала обязательны и должны быть разными.

---

==== TASK-07: Геофильтрация подборок

*Что сейчас:* подборка формируется без учёта страны работодателя.

*По ТЗ (§2, §5.4):* работодатель видит кандидатов из своей страны + готовых к релокации туда.

*Что нужно сделать (MVP — ручной вариант):*

* Добавить поле «основная страна» в карточку работодателя.
* В редакторе: отображать страну работодателей-подписчиков при формировании выпуска, чтобы редактор мог учесть это вручную.
* Позже: автоматическая фильтрация кандидатов по стране при формировании выпуска.

---

=== 🟡 Нужно для полноты MVP

==== TASK-10: Приветственное письмо работодателю

*Зависит от:* TASK-09 ✅

*По ТЗ (§14):* после подтверждения регистрации работодатель получает письмо с описанием механики работы сервиса и информацией о пробном периоде (1 месяц).

*Что нужно сделать:*

* Добавить шаблон приветственного письма (текст в §14 ТЗ).
* Вызывать через SendPulse в `confirmEmployer()` (`app/actions.ts`) — место для вызова уже помечено комментарием `TASK-10`.

---

==== TASK-11: Процесс обработки анкеты кандидата

*Что сейчас:* форма кандидата сразу записывает в Sheets — без модерации.

*По ТЗ (§14):* анкета → статус «На проверке» → рекрутер решает добавить или отклонить → «Активный» с датой начала.

*Что нужно сделать:*

* Добавить колонку «Статус» в лист Candidates.
* При регистрации: сохранять со статусом «На проверке».
* Уведомление рекрутера о новой анкете (email).
* В редакторе: список анкет кандидатов на проверке, кнопки «Добавить в базу» / «Отклонить».

---

==== TASK-12: Summary кандидата в теле email-рассылки

*Что сейчас:* письмо содержит только ссылку на страницу подборки.

*По ТЗ (§10):* в анонсе после вступительного текста показывается *первое Summary* кандидата из выпуска, чтобы дать работодателю предпросмотр до перехода по ссылке.

*Что нужно сделать:*

* В `publishMailingList` (`app/actions.ts`): получить первого кандидата из выпуска.
* Добавить его анонимное Summary в HTML тела письма перед кнопкой «Смотреть всех кандидатов».

---

==== TASK-14: Уведомление работодателя о результате запроса на контакт

*Зависит от:* TASK-02 (статусы запросов).

*⚠️ Открытый вопрос:* канал уведомления не определён. Рассматриваются два варианта:

* *Вариант A (ручной)* — менеджер уведомляет работодателя самостоятельно; никакой автоматики не требуется.
* *Вариант B (автоматический)* — отправка через SendPulse при смене статуса в TASK-02.

Решение принимается при реализации TASK-02. До принятия решения задача находится в режиме ожидания.

*Что нужно сделать (если выбран вариант B):*

. При смене статуса на «Кандидату интересно» — отправить работодателю письмо через SendPulse с предложением связаться.
. При смене статуса на «Кандидату не интересно» — отправить письмо с текстом из ТЗ (§18.2).
. Убедиться, что email работодателя доступен в листе «Contact Requests» (берётся из профиля по токену).

*Затрагивает (вариант B):* `app/actions.ts`, `lib/sendpulse.ts`.

---

==== TASK-15: Дополнительные страны работодателя

*По ТЗ (§7, карточка работодателя):* работодатель может указать несколько стран, из которых готов рассматривать кандидатов. Это расширяет TASK-07 (геофильтрация).

*Что нужно сделать:*

. Добавить поле «Дополнительные страны» в форму регистрации работодателя (мультиселект).
. Сохранять в Google Sheets (новая колонка `Additional Countries`).
. Учитывать при геофильтрации в TASK-07: работодатель видит кандидатов из основной страны ИЛИ из дополнительных стран.

*Затрагивает:* форма работодателя, `app/actions.ts`, `lib/sheets.ts`, TASK-13 (фильтрация на странице подборки).

---

==== TASK-16: Чекбоксы согласия на обработку персональных данных

*По ТЗ (§14, чекбоксы согласия):* на формах регистрации обязательны чекбоксы.

*Чекбоксы:*

* Работодатель: «Я подтверждаю согласие на обработку персональных данных и получение информационных сообщений сервиса.»
* Кандидат: «Я подтверждаю согласие на обработку персональных данных и рассмотрение моей кандидатуры для публикации в рассылках сервиса.»

*Что нужно сделать:*

. Добавить чекбокс в `EmployerRegistrationModal` — форма не отправляется если не отмечен.
. Добавить чекбокс в `CandidateRegistrationModal` — аналогично.

*Затрагивает:* `components/employer-registration-modal.tsx`, `components/candidate-registration-modal.tsx`.

---

==== TASK-17: Отслеживание SLA и эскалация запроса «Хочу связаться»

*По ТЗ (§12, шаг 3):* ответ работодателю — не позднее 4 рабочих дней. Если кандидат не отвечает 3 рабочих дня — эскалация администратора.

*Что нужно сделать:*

. В интерфейсе запросов (TASK-02) выделять цветом строки, по которым срок ответа истекает или уже истёк (на основе `Timestamp` из листа «Contact Requests»).
. Добавить вычисляемое поле «Дедлайн» (Timestamp + 4 рабочих дня) для отображения в редакторе.
. Статус «Кандидат не отвечает» через 3 рабочих дня автоматически помечать как «требует эскалации» (визуально в редакторе).

*Затрагивает:* `app/editor/requests/page.tsx` (TASK-02), `lib/sheets.ts`.

---

=== 🟡 Нужно для полноты MVP (продолжение)

==== TASK-18: Дата уточнения готовности кандидата

*По ТЗ (§8, карточка кандидата):* через 3 месяца после последней публикации необходимо уточнить готовность кандидата к диалогу.

*Что нужно сделать:*

. Добавить в Profile поле `readinessCheckDate` — дата, когда нужно связаться с кандидатом (последняя публикация + 3 месяца).
. В редакторе (раздел Кандидаты, TASK-11) показывать кандидатов, у которых `readinessCheckDate` наступила или наступает в ближайшие 7 дней.

*Затрагивает:* `lib/sheets.ts`, TASK-05 (правила публикации), TASK-11 (раздел кандидатов в редакторе).

---

==== TASK-19: Загрузка файла резюме в форме кандидата

*По ТЗ (§16):* «Резюме — прикрепить файл или указать ссылку на LinkedIn». Сейчас только ссылка.

*Что нужно сделать:*

. Добавить input type=file в форму кандидата.
. Загружать файл в облачное хранилище (Google Drive через Service Account или S3-совместимое).
. Сохранять ссылку на файл в Google Sheets вместо/вместе с ссылкой на LinkedIn.

*Затрагивает:* `components/candidate-registration-modal.tsx`, `app/actions.ts`.

---

==== TASK-20: Письмо кандидату со ссылкой на анкету

*По ТЗ (§14, NOTE):* кнопка «Я кандидат» → не прямая форма, а email со ссылкой на анкету.

*Что нужно сделать:*

. На главной странице заменить прямой переход к форме на поле «Введите email» + кнопку «Получить ссылку».
. Server Action отправляет письмо через SendPulse со ссылкой на `/candidate-register` (или на главную с открытой формой кандидата).

*Затрагивает:* `app/page.tsx`, `app/actions.ts`, `lib/sendpulse.ts`.

---

==== TASK-21: Предупреждение о количестве кандидатов в выпуске

*По ТЗ (§10):* в выпуске должно быть от 6 до 10 кандидатов.

*Что нужно сделать:*

. В разделе «Выпуски» редактора показывать визуальное предупреждение если `candidateCount` < 6 или > 10.
. Не блокировать отправку, только информировать редактора.

*Затрагивает:* `app/editor/page.tsx`.

---

=== ⚪ Вне MVP (задокументировано, реализовать позже)

[cols="1,3", options="header"]
|===
| Задача | Примечание

| Telegram-рассылка
| По ТЗ «обязательно, но можно вручную» — приемлемо для MVP

| WhatsApp-рассылка
| По ТЗ через бизнес-аккаунт — требует отдельной интеграции

| Автоматический маппинг тегов → стримы
| Сейчас редактор вручную определяет попадание в стримы; автоматизация — следующий этап

| Биллинг и управление подпиской
| Явно исключено из MVP в ТЗ

| Аналитика
| Явно исключено из MVP в ТЗ
|===

== Приоритизация

[cols="1,1,1,2", options="header"]
|===
| ID | Приоритет | Сложность | Примечание

| TASK-14 | 🔴 Критично | Низкая | Зависит от TASK-02 ✅; завершает цикл обработки запросов
| TASK-16 | 🟠 Важно | Низкая | Требование по ПД, простая реализация
| TASK-05 | 🟠 Важно | Средняя | Зависит от TASK-04 ✅; блокирует TASK-18
| TASK-15 | 🟠 Важно | Низкая | Расширяет TASK-07; нужна для корректной геофильтрации
| TASK-06 | 🟠 Важно | Низкая | Простое изменение формы
| TASK-07 | 🟠 Важно | Низкая | Зависит от TASK-15; MVP — только отображение
| TASK-17 | 🟠 Важно | Средняя | Зависит от TASK-02 ✅; SLA по ТЗ
| TASK-10 | 🟡 MVP | Низкая | Зависит от TASK-09 ✅
| TASK-11 | 🟡 MVP | Средняя | Аналогично TASK-09 ✅; блокирует TASK-18
| TASK-12 | 🟡 MVP | Средняя | Зависит от TASK-03 ✅, TASK-04 ✅, TASK-13 ✅
| TASK-18 | 🟡 MVP | Низкая | Зависит от TASK-05, TASK-11
| TASK-19 | 🟡 MVP | Средняя | Независимая; требует облачного хранилища
| TASK-20 | 🟡 MVP | Средняя | Независимая
| TASK-21 | 🟡 MVP | Низкая | Независимая; только предупреждение в UI
|===

== Рекомендуемая очерёдность

. *TASK-16* — чекбоксы согласия (быстро, требование по ПД).
. *TASK-14* — уведомление работодателя о результате (TASK-02 ✅; завершает цикл).
. *TASK-17* — SLA и эскалация запросов (TASK-02 ✅).
. *TASK-10* — приветственное письмо работодателю (место в коде уже есть).
. *TASK-06* — два канала доставки (быстро).
. *TASK-15* + *TASK-07* — дополнительные страны + геофильтрация.
. *TASK-05* — правила публикации.
. *TASK-21* — предупреждение о количестве кандидатов (быстро).
. *TASK-11* — модерация анкет кандидатов.
. *TASK-18* — дата уточнения готовности.
. *TASK-12* — Summary в теле письма.
. *TASK-20* — письмо кандидату со ссылкой на анкету.
. *TASK-19* — загрузка файла резюме.
