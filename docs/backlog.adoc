= TalentStreams — Анализ и бэклог доработок
:toc: left
:toc-title: Содержание
:toclevels: 3
:sectnums:
:icons: font
:lang: ru

NOTE: ℹ️ Документ разделён на две части: <<Выполнено>> — всё, что реализовано и проверено в текущем коде, и <<Активные задачи (требуется сделать)>> — всё, что ещё предстоит сделать. Не путайте их: раздел «Активные задачи» не содержит выполненных пунктов.

== Выполнено

=== Обзор по компонентам

[cols="1,3", options="header"]
|===
| Компонент | Статус

| Главная страница `/`
| Реализована: hero, описание механики, формы регистрации работодателя и кандидата. Фавикон (`app/icon.tsx`) — тот же логотип, что в хедере: `bg-primary`-квадрат с белой молнией (Zap из lucide-react, тот же SVG-путь), сгенерирован через `next/og` `ImageResponse`, автоматически подключается Next.js во все страницы.

| Страница подборки `/list/[listId]`
| Реализована: анонимные карточки кандидатов (без имени и контактов), теги, summary, disclaimer о конфиденциальности. Кнопка «Хочу связаться» — функциональна: записывает запрос в лист «Contact Requests». Блок «Не нашли подходящих кандидатов?» — виден только при наличии токена работодателя (`?e=`). Noindex. Доступ только по `?e=[employer_token]` (персонализированная подборка) или `?secret=[EDITOR_SECRET]` (редактор); без параметров — 404.

| Страница профиля `/profile/[id]`
| Реализована: полный профиль кандидата. Недоступна с публичных страниц. Noindex.

| Редактор `/editor`
| Реализован: боковое меню с разделами (Выпуски / Работодатели / Кандидаты / Запросы / Стримы / Настройки). Выпуски: история кампаний, проверка пустой книги, ссылка на подборку с секретом. Работодатели и Кандидаты (TASK-30 ✅): панель фильтров сверху страницы (поиск, статус — по умолчанию «Все статусы», стрим, для работодателей ещё страна, для кандидатов — уровень) вместо коллапсируемых секций; кнопки «Подтвердить»/«Отклонить» и «Добавить»/«Отклонить»; для уже подтверждённых/активных — «Отключить», для отклонённых — снова «Подтвердить»; корзина с отдельным шагом подтверждения удаляет запись (у работодателя — из Neon + отписка от SendPulse, TASK-31-доп.; у кандидата — из Sheets + история резюме из Neon + файлы из Vercel Blob, TASK-33 ✅); «+ Добавить кандидата» и «+ Добавить работодателя» заводят новую запись без публичной формы, сразу через ту же валидацию, что и публичная форма (у работодателя — переиспользует `registerEmployer()` целиком, включая проверку дублей email/телефона), но статус остаётся «На проверке» — заносить может один человек, подтверждать другой (TASK-33 ✅ для кандидата, отдельная небольшая доработка для работодателя — см. <<Выполнено>>). Кнопка-карандаш на каждой строке открывает форму редактирования (у кандидатов — с TASK-23, у работодателей — TASK-31 ✅). Запросы: управление запросами на контакт с двумя разделами («Общие запросы» и «По кандидатам»), цветные бейджи статусов, смена статуса через выпадающий список. Настройки: пусто — задел на будущее (кнопка миграции колонок Google Sheets убрана, дальше не нужна). Все страницы `/editor/*` — на всю ширину, без `max-w`.

| SendPulse интеграция
| Реализована: адресные книги (авто-создание), email-кампании, пагинация, TTL кэша книг (60 с). Проверка пустой книги до запуска рассылки.

| API `/api/publish/[listId]`
| Реализован: curl-triggered публикация с авторизацией по токену

| Регистрация работодателя
| Реализована: форма → Neon со статусом «На проверке» (TASK-DB-3 ✅ — раньше писала в Google Sheets). В SendPulse не добавляется — только после подтверждения редактором.

| Подтверждение работодателя
| Реализовано: в `/editor` редактор подтверждает или отклоняет заявку. При подтверждении — добавление в SendPulse (мастер-книга + книги выбранных стримов).

| Регистрация кандидата
| Реализована: форма → Google Sheets (SendPulse не используется). Резюме: загрузка файла (PDF, DOC, DOCX, RTF, ODT, до 5 МБ) в Vercel Blob через `POST /api/upload` или ввод ссылки вручную — альтернативы. Доступ к файлу через прокси-роут `GET /api/resume` с авторизацией по Bearer-токену. URL резюме в Sheets — ссылка на прокси (`APP_URL/api/resume?url=...`), всегда только последняя версия. Полная история версий резюме (файлы и ссылки, с датами) хранится в Neon и видна в редакторе (TASK-32 ✅) — старые blob-файлы при замене больше не теряются и не остаются мусором без ссылок.

| Google Sheets интеграция
| Реализована: чтение профилей, подборок, стримов (стримы и работодатели уже перенесены в Neon — TASK-DB-8, TASK-DB-3); запись регистраций кандидатов; авто-миграция колонок кандидатов (`ensureCandidateColumns`, вызывается внутри `appendCandidateRow` при каждой записи — не разовая ручная операция). `GOOGLE_SERVICE_ACCOUNT_JSON` парсится устойчиво к сырым переносам строк в `private_key` (`safeJsonParse()` — типичный артефакт `vercel env pull`); ошибки конфигурации теперь конкретные (что именно не задано/невалидно), а не общая фраза.

| Robots / noindex
| Реализовано: `app/robots.ts` (Disallow: /list/, /profile/), meta robots noindex на страницах подборки и профиля

| Персонализация рассылок
| Реализована: токен работодателя (`crypto.randomUUID()`, поле `token` в таблице `employers` Neon — TASK-DB-3 ✅); `employer_token` передаётся в SendPulse; ссылка в письме `/list/[listId]?e={{employer_token}}`; `getEmployerByToken()` и `filterCandidatesForEmployer()` в `lib/db/employers.ts`. Фильтрация — по `excludedCompanies`/`excludedIndustries` и по гео (`countryPrimary`/`countryDesired` кандидата vs `country`/`additionalCountries` работодателя, TASK-24 ✅). Гео-сегментация самой рассылки (какие подписчики получат письмо) пока не реализована — TASK-25.

| Запросы на контакт (Contact Requests)
| Хранятся в PostgreSQL (Neon) через Drizzle ORM — таблица `contactRequests`. Два типа: по кандидату (`candidateId` заполнен, 7 статусов) и общий (`candidateId` пуст, 3 статуса). `employerToken` и `streamId` — внешние ключи на `employers`/`streams` (не дублирующие текстовые поля, см. ниже). Функции в `lib/db/contact-requests.ts`. Исторические данные в Google Sheets остаются как архив.

| Neon + Drizzle ORM
| ✅ TASK-DB-1 + TASK-DB-2. PostgreSQL через Vercel Marketplace (Neon). Схема — `lib/db/schema.ts`, клиент — `lib/db/index.ts` (neon-http driver). Миграция — `scripts/migrate.mjs`.

| Стримы — редактор (Neon)
| ✅ TASK-DB-8. Стримы перенесены из Google Sheets в Neon. Миграция `0001_real_wendell_vaughn.sql` содержит схему + начальные данные (11 стримов: 6 Industry, 5 Functional). `lib/db/streams.ts` — полный CRUD. Страница `/editor/streams` — полноценный редактор: inline-редактирование (название, тип — выпадающий список Industry/Functional, описание), удаление строки, добавление нового стрима. Компонент `StreamsTable` (client). Копирование названия в буфер обмена. Цветные бейджи типов (синий / фиолетовый).
|===

=== Детали по выполненным задачам

==== ✅ TASK-04: Пять структурированных тегов кандидата

*Выполнено:*

* Добавлены поля `level`, `industry`, `func`, `countryPrimary`, `countryDesired` в тип `Profile` (`lib/sheets.ts`).
* Добавлены алиасы заголовков на английском и русском в `HEADER_ALIASES` (`Level`/`Уровень`, `Industry`/`Отрасль`, `Function`/`Функция`, `Country Primary`/`Страна текущая`/`Основная страна`, `Country Desired`/`Страна желаемая`/`Желаемая страна`).
* Поля читаются из Google Sheets в `buildProfile`.
* Добавлена функция `ensureProfileColumns()` (`lib/sheets.ts`) — проверяет наличие колонок в таблице и дописывает недостающие.
* Добавлена кнопка «Проверить или добавить колонки в таблицу» в редакторе (`components/add-columns-button.tsx`).

---

==== ✅ TASK-03: Анонимизация кандидатов на странице подборки

*Выполнено:*

* Карточка кандидата в `/list/[listId]/page.tsx` переработана: убраны имя, ссылка на полный профиль и прямые контакты.
* Показываются только анонимные данные: должность (`title`), страна (`countryPrimary` + `countryDesired`), теги (`level`, `industry`, `func`) и текст `summary`.
* Поле `summary` добавлено в тип `Profile`, алиасы `HEADER_ALIASES`, `buildProfile` и в миграцию колонок (`ensureProfileColumns`).
* Профиль кандидата перенесён с `/[id]` на `/profile/[id]`; со страницы подборки прямых ссылок нет.
* Добавлен мета-тег `robots: noindex, nofollow` на страницах `/list/[listId]` и `/profile/[id]`.
* Добавлен `app/robots.ts` с `Disallow: /list/, /profile/`.
* Добавлена кнопка-заглушка «Хочу связаться» (`components/contact-button.tsx`) — готова к реализации в TASK-01.

---

==== ✅ TASK-09: Процесс подтверждения регистрации работодателя

*Выполнено:*

* `Status` добавлен в `EMPLOYERS_HEADERS`; `registerEmployer` пишет «На проверке» и не добавляет в SendPulse.
* Добавлены `getEmployers()` и `updateEmployerStatus(rowIndex, status)` в `lib/sheets.ts`.
* Добавлены Server Actions `confirmEmployer()` (сначала SendPulse, затем статус) и `rejectEmployer()` с `revalidatePath("/editor")`.
* Компонент `EmployerSection` в редакторе: три коллапсируемые группы — «На проверке» (открыта), «Отклонён» и «Подтверждён» (свёрнуты). Ошибки показываются inline под строкой работодателя.

---

==== ✅ TASK-13: Персонализация рассылок — токен работодателя и предпочтения кандидата

*Выполнено:*

*Часть 1 — токен работодателя:*

* Колонка `ID` добавлена в лист Employers; `registerEmployer` генерирует `crypto.randomUUID()` (хранится как `ID` в Sheets, используется как токен работодателя).
* `appendEmployerRow` читает реальные заголовки листа перед записью (header-mapped write, не позиционный) — работает при любом порядке колонок.
* `confirmEmployer` передаёт переменную `employer_token` в контакт SendPulse.
* URL шаблона письма обновлён: `/list/[listId]?e={{employer_token}}`.
* Добавлен `getEmployerByToken(token)` в `lib/sheets.ts`.
* Тип `Employer` теперь содержит поле `token: string`.

*Часть 2 — предпочтения кандидата:*

* Тип `Profile` теперь содержит поля `excludedCompanies: string[]` и `excludedIndustries: string[]`.
* `HEADER_ALIASES` дополнен алиасами: `"excluded companies"` → `excludedCompanies`, `"excluded industries"` → `excludedIndustries` (и русские варианты).
* `DISTRIBUTION_COLUMNS` обновлён: добавлены `"Excluded Companies"` и `"Excluded Industries"` — кнопка «Настройки» добавит их в таблицу.
* Добавлена `filterCandidatesForEmployer(candidates, employer)` в `lib/sheets.ts`.

*Часть 3 — персонализированная страница подборки:*

* `/list/[listId]` теперь требует `?e=[token]` или `?secret=[EDITOR_SECRET]`.
* `?e=[token]`: работодатель находится через `getEmployerByToken`, кандидаты фильтруются через `filterCandidatesForEmployer`; невалидный/отсутствующий токен → 404.
* `?secret=[EDITOR_SECRET]`: все кандидаты показываются без фильтрации.
* Без параметров → `notFound()` (404).
* `employerToken` передаётся в `ContactButton` (готово для TASK-01).

*Дополнительно:* регистрация теперь проверяет уникальность email и телефона (среди статусов «На проверке» и «Подтверждён»), телефон сравнивается без нецифровых символов.

---

==== ✅ TASK-01: Кнопка «Хочу связаться» на странице подборки

*Выполнено:*

* `appendContactRequest()` добавлена в `lib/sheets.ts` — записывает строку в лист «Contact Requests» с полями: ID (UUID), Timestamp, List ID, Stream, Candidate ID, Employer Token, Employer Name, Company, Employer Email, Status («Новый запрос»). Лист создаётся автоматически при первом обращении через `ensureSheet`.
* Server Action `submitContactRequest(candidateId, listId, employerToken)` добавлен в `app/actions.ts` — находит работодателя по токену через `getEmployerByToken`, получает стрим через `getMailingLists`, вызывает `appendContactRequest`.
* `ContactButton` (`components/contact-button.tsx`) полностью функциональна: вызывает Server Action, показывает «Отправка…» в процессе, подтверждение об успехе, inline-ошибку с иконкой AlertCircle. При отсутствии `employerToken` (вид редактора): кнопка неактивна, метка «работодатель не определён».

NOTE: ℹ️ `appendContactRequest()` перенесена в Neon в рамках TASK-DB-2 ✅. Актуальная реализация — `lib/db/contact-requests.ts`.

---

==== ✅ TASK-02: Интерфейс управления запросами в редакторе

*Выполнено:*

* Типы `ContactRequest` и `ContactRequestStatus` определены (изначально в `lib/sheets.ts`, перенесены в `lib/db/contact-requests.ts` при TASK-DB-2).
* `getContactRequests()` — выборка запросов, сортировка по убыванию даты.
* `updateContactRequestStatus(id, status)` — обновление статуса по `id` (UUID).
* Server Action `setContactRequestStatus(id, status)` добавлен в `app/actions.ts` с `revalidatePath("/editor")`.
* Страница `app/editor/requests/page.tsx` — загружает запросы и профили параллельно, показывает бейдж «N новых».
* `components/contact-requests-section.tsx` — два раздела:
** «Общие запросы»: запросы без `candidateId` — 3 статуса (Новый запрос / Взяли в работу / Завершён).
** «По кандидатам»: запросы с `candidateId` — 7 статусов (Новый запрос, Кандидату написали, Кандидат не отвечает, Связаться с Аленой, Кандидату интересно, Кандидату не интересно, Контакты переданы).
** Цветные бейджи статусов; `<select>` выпадающий список меняет статус через `useTransition`.
* «Запросы» добавлен в `EditorNav` (иконка Handshake, между Работодатели и Настройки).

NOTE: ℹ️ Реализация перенесена в Neon (TASK-DB-2 ✅). `lib/sheets.ts` больше не содержит функций Contact Requests. Актуальные функции — `lib/db/contact-requests.ts`. `setContactRequestStatus` принимает `id: string` (UUID) вместо `rowIndex: number`.

---

==== ✅ TASK-08: Блок «Не нашли подходящих кандидатов?»

*Выполнено (реализация отличается от исходного ТЗ):*

* Блок добавлен в нижнюю часть `/list/[listId]` — виден только при наличии `employerToken` (не отображается при доступе редактора через `?secret=`).
* `GeneralInquiryButton` (`components/general-inquiry-button.tsx`) вызывает `submitGeneralInquiry(listId, employerToken)`.
* `submitGeneralInquiry` пишет в лист «Contact Requests» с `candidateId: ""` (пустой) — отдельная форма не нужна, т. к. данные работодателя уже известны из токена.
* Отправка на email администратора *не реализована* — запросы хранятся в «Contact Requests» и отображаются в редакторе в разделе «Общие запросы» с 3 статусами.
* `ContactRequestStatus` расширен статусами «Взяли в работу» и «Завершён».

---

==== ✅ TASK-16: Чекбоксы согласия на обработку персональных данных

*Выполнено:*

* Чекбокс добавлен в `EmployerRegistrationModal` (`components/employer-registration-modal.tsx`) — текст: «Я подтверждаю согласие на обработку персональных данных и получение информационных сообщений сервиса.» Кнопка «Отправить заявку» заблокирована, пока чекбокс не отмечен.
* Чекбокс добавлен в `CandidateRegistrationModal` (`components/candidate-registration-modal.tsx`) — текст: «Я подтверждаю согласие на обработку персональных данных и рассмотрение моей кандидатуры для публикации в рассылках сервиса.» Аналогично блокирует отправку.
* При закрытии модала чекбокс сбрасывается вместе с остальными полями.

---

==== ✅ TASK-07 + TASK-15: Страна работодателя и дополнительные страны

*Выполнено:*

* Добавлены поля `country: string` и `additionalCountries: string[]` в тип `Employer` (`lib/sheets.ts`).
* `parseEmployerRows` считывает колонки `Country` и `Additional Countries` по имени (header-mapped).
* `ensureEmployerColumns()` добавлена в `lib/sheets.ts` — идемпотентно добавляет колонки `Country` и `Additional Countries` в лист Employers. Вызывается из `addAllColumns()` в `app/actions.ts` — единая кнопка «Проверить или добавить колонки» в Настройках покрывает все листы (профили, работодатели, кандидаты).
* Форма регистрации работодателя (`EmployerRegistrationModal`) переработана: расширена до `max-w-2xl` с двухколоночной раскладкой (Имя + Компания, Email + Телефон, Стримы + Основная страна). Основная страна — `<select>` из 4 стран СНГ (Казахстан, Узбекистан, Кыргызстан, Таджикистан). Дополнительные страны — toggle-кнопки (полный список СНГ) + кнопка «Любая» (при выборе снимает все остальные).
* В карточке редактора (`EmployerSection`) страна и дополнительные страны отображаются в строке под именем.
* Геофильтрация на странице подборки реализована (TASK-24 ✅): `filterCandidatesForEmployer()` сопоставляет `countryPrimary`/`countryDesired` кандидата со странами работодателя; «Любая» снимает ограничение. Гео-сегментация получателей рассылки — TASK-25, ждёт решения о переходе на Unisender.
* Диапазон чтения Employers расширен с `A1:L1000` до `A1:Z1000`; в `updateEmployerStatus` диапазон заголовка исправлен с `A1:J1` до `A1:Z1` (ранее пропускал колонку Status).

---

==== ✅ TASK-11: Процесс обработки анкеты кандидата

*Выполнено (без email-уведомления рекрутера — отложено):*

* Добавлены типы `CandidateStatus = "На проверке" | "Активный" | "Отклонён"` и `Candidate` в `lib/sheets.ts`.
* `appendCandidateRow` — header-mapped запись `Record<string, string>` с case-insensitive сопоставлением заголовков.
* `registerCandidate` (`app/actions.ts`) записывает анкету со статусом `"На проверке"`.
* `getCandidates()` — читает главный лист профилей, возвращает все статусы для редактора.
* `updateCandidateStatus(rowIndex, status, activeSince?)` — обновляет Status и Active Since по заголовку в главном листе.
* `ensureCandidateColumns()` — идемпотентно добавляет колонки в главный лист; включена в `addAllColumns()`.
* Server Actions `approveCandidate(rowIndex)` и `rejectCandidate(rowIndex)` с `revalidatePath("/editor/candidates")`.
* Страница `app/editor/candidates/page.tsx` — auth-проверка, загрузка и разбивка по статусам.
* `components/candidate-section.tsx` — три коллапсируемые группы. «На проверке» открыта по умолчанию; «Активные» открывается автоматически если «На проверке» пустая — так legacy-профили без колонки Status сразу видны. Обе группы показывают badge с количеством. Кнопки «Добавить» (emerald) / «Отклонить» (destructive) с `useTransition` и оптимистичным локальным статусом.
* Пункт «Кандидаты» добавлен в `EditorNav` (иконка Users, между «Работодателями» и «Запросами»).

---

==== ✅ TASK-22: Слияние Candidates и Candidates Database в один лист

*Выполнено:*

*Архитектурное решение:* регистрационная форма и рассылки теперь работают с одним листом (первый лист / «Candidates Database»). Отдельный лист «Candidates» упразднён.

* `CandidateStatus` перемещён в начало `lib/sheets.ts` — используется в типе `Profile`.
* Тип `Profile` расширен регистрационными полями: `status: CandidateStatus | ""`, `activeSince`, `registrationTimestamp`, `coverLetter`, `resumeUrl`. Пустой `status` = legacy-строка без колонки → считается «Активный».
* `KNOWN_KEYS` и `HEADER_ALIASES` дополнены алиасами: `"status"`, `"active since"` → `activeSince`, `"timestamp"` → `registrationTimestamp`, `"cover letter"` → `coverLetter`, `"resume url"` → `resumeUrl`.
* `buildProfile()` читает все новые поля из строки.
* `rowsToProfiles()` (а значит `getProfiles()`) фильтрует: возвращает только строки где `status` пустой или «Активный» — кандидаты «На проверке» и «Отклонён» в рассылки не попадают.
* `appendCandidateRow()` пишет в главный лист (без префикса `'Candidates'!`), case-insensitive сопоставление заголовков — работает при любом регистре заголовков редактора.
* `getCandidates()` читает из `SHEET_RANGE` (главный лист) через `rowToRecord()` — применяет `HEADER_ALIASES` (включая русские алиасы «Кандидат» → `name`, «Статус» → `status` и т.д.), поэтому работает с любым регистром и языком заголовков, которые редактор задал вручную. Пустой статус → «Активный» (обратная совместимость с legacy-профилями без колонки Status).
* `updateCandidateStatus()` обновляет ячейки главного листа.
* `ensureCandidateColumns()` добавляет в главный лист колонки: `id`, `Status`, `Active Since`, `Timestamp`, `Cover Letter`, `Resume URL`.
* `registerCandidate()` генерирует UUID (`crypto.randomUUID()`), передаёт в `appendCandidateRow` с lowercase-ключами для надёжного case-insensitive маппинга.

---

==== ✅ TASK-23: Редактирование данных кандидата в редакторе

*Выполнено:*

* `lib/sheets.ts` — `updateCandidateFields(rowIndex, data)`: читает заголовки главного листа, разрешает алиасы через `mapHeader()`, обновляет ячейки полей параллельно через Sheets API PUT. Колонки находятся по каноническому имени — работает с любым языком заголовков.
* `app/actions.ts` — `updateCandidate(rowIndex, data)`: валидирует имя и email, вызывает `updateCandidateFields`, инвалидирует `/editor/candidates`.
* `components/candidate-edit-modal.tsx` — модальная форма редактирования: предзаполнена данными кандидата. Переключатель «Загрузить файл / Указать ссылку» — при выборе файла загрузка в Vercel Blob происходит в момент сабмита (через `/api/upload`). Escape закрывает. После сохранения — экран подтверждения.
* `components/candidate-section.tsx` — кнопка-карандаш (Pencil) на каждой строке кандидата (доступна для всех статусов). При клике открывается `CandidateEditModal`. Кнопки «Добавить» / «Отклонить» остались только для статуса «На проверке».

*Редактируемые поля (после двух расширений — см. ниже):* имя, роль, email, телефон, резюме (файл/URL), уровень, стримы (multi-select), страна текущая/желаемая, summary, сопроводительное письмо, дата активности.

**Расширение 1 (уровень, стримы, страны, дата активности):**

* `lib/sheets.ts` — `Candidate` дополнен `stream: string[]`, `level: string`, `countryPrimary: string`, `countryDesired: string` (данные уже читались через `rowToRecord()`/`HEADER_ALIASES` для `Profile`, просто не попадали в возвращаемый объект `Candidate`); `updateCandidateFields()` принимает эти поля плюс `activeSince` — та же generic mapHeader-логика, менять не пришлось.
* `components/candidate-edit-modal.tsx` — уровень (текст), стримы (те же toggle-кнопки, что в форме работодателя, канонический список из `getStreams()`), страна текущая/желаемая (`<select>` из `ADDITIONAL_COUNTRIES`, вынесен из `employer-registration-modal.tsx`), активен с (`<input type="date">`).
* `activeSince` хранится в ru-RU формате (`21.07.2026`, как пишет `approveCandidate()`) — `<input type="date">` требует ISO; конвертация `toISODate()`/`toRuDate()` на границе компонента, формат хранения не меняется.
* Модалка расширена до `max-w-lg` под двухколоночные поля.

**Расширение 2 (роль и summary):** до этого «Роль» (title) и «Summary» — единственные два содержательных поля, которые видит работодатель на карточке подборки — правились только напрямую в Google Sheets, никакой формы для них не было.

* `lib/sheets.ts` — `Candidate` дополнен `title: string`, `summary: string`. Оба поля уже резолвились через `HEADER_ALIASES` для `Profile` (`роль` → `title`, `саммари`/`summary` → `summary`) — расширение чисто типовое, runtime-логика `updateCandidateFields()`/`rowToRecord()` не менялась.
* `components/candidate-edit-modal.tsx` — «Роль» (текстовое поле сразу после «Имя»), «Summary» (textarea перед блоком «Резюме»).

**Расширение 3 (широкая форма, раздельное добавление файла/ссылки):** после TASK-32 форма стала слишком длинной в одну колонку — приходилось много прокручивать. Плюс переключатель «Загрузить файл / Указать ссылку» вёл себя не всегда ожидаемо: переключение вкладки очищало значение другого поля (`setResumeUrl("")` при переходе на «файл»), так что случайный клик по вкладке без выбора нового файла мог обнулить уже сохранённое резюме при сабмите.

* `components/candidate-edit-modal.tsx` — модалка расширена с `max-w-lg` до `max-w-2xl` (как у `EmployerEditModal`), поля переложены в `grid grid-cols-2` (Имя/Роль, Email/Телефон, Уровень/Активен с, страны — парами; Стримы/Summary/Резюме/Сопроводительное письмо — на всю ширину через `col-span-2`). Форма — `max-h-[75vh] overflow-y-auto`, так что скроллится сама модалка, а не вся страница редактора.
* Резюме переосмыслено: вместо переключателя «режим ввода», перезаписывающего общее поле, — независимая пара кнопок «+ Добавить файл» / «+ Добавить ссылку» (`resumeAction: "none" | "file" | "link"`). Пока `resumeAction === "none"`, `candidate.resumeUrl` не трогается вообще — только показывается как «Текущее резюме» со ссылкой «Открыть». Выбор одной из кнопок открывает соответствующий ввод (file picker или `<input type="url">`) плюс кнопку «Отмена», возвращающую к «none» без побочных эффектов. На сабмите итоговый `resumeUrl` берётся из `candidate.resumeUrl`, только если `resumeAction` так и остался `"none"` — иначе из загруженного файла/введённой ссылки.

---

==== ✅ TASK-19: Загрузка файла резюме в форме кандидата

*Выполнено:*

* `@vercel/blob` установлен; хранилище — private Vercel Blob store.
* `app/api/upload/route.ts` — принимает `POST multipart/form-data`, валидирует тип (PDF, DOC, DOCX, RTF, ODT) и размер (≤ 5 МБ), загружает через `put(filename, file, { access: "private" })`. Имя файла: `resumes/{timestamp}-{uuid8}.{ext}`.
* `app/api/resume/route.ts` — прокси-роут `GET ?url=<blobUrl>`: проверяет hostname (`*.blob.vercel-storage.com`), скачивает файл server-side с `Authorization: Bearer BLOB_READ_WRITE_TOKEN`, стримит клиенту с корректным `Content-Type`.
* В Sheets сохраняется прокси-URL (`APP_URL/api/resume?url=...`) — ссылка открывается и из редактора, и из Google Sheets.
* `CandidateRegistrationModal` — переключатель «Загрузить файл / Указать ссылку»: файл выбирается без немедленной загрузки, загрузка происходит в момент сабмита формы (spinner в кнопке покрывает оба этапа). Длинное имя файла обрезается с `truncate`.
* `CANDIDATE_EXTRA_COLUMNS` дополнен `"Email"` и `"Phone"`; `appendCandidateRow` автоматически вызывает `ensureCandidateColumns()` перед записью — колонки создаются при первой регистрации если отсутствуют.
* `appendCandidateRow` обновлён: при маппинге заголовков листа применяется `mapHeader()` (алиас «Кандидат» → `name` и т.д.) — email и имя теперь корректно пишутся независимо от языка заголовков.

*Безопасность:* прокси-роут без аутентификации — защита только через неугадываемость UUID в URL. TASK-SEC-1 (добавить проверку EDITOR_SECRET) — отложено, см. <<Вне MVP (задокументировано, реализовать позже)>>.

---

==== ✅ TASK-21: Предупреждение о количестве кандидатов в выпуске

*Выполнено:*

* В карточке выпуска рядом со счётчиком кандидатов появляется янтарный badge с иконкой `AlertTriangle`:
** `candidateCount < 6` → «Мало кандидатов (менее 6)»
** `candidateCount > 10` → «Много кандидатов (более 10)»
* Отправка не блокируется — только информирование. Затронут `app/editor/page.tsx`.

---

==== ✅ TASK-10: Приветственное письмо работодателю

*Выполнено:*

* HTML-шаблон письма создан в `docs/welcome-email-template.html` — для загрузки в редактор шаблонов SendPulse.
* Письмо отправляется автоматикой SendPulse при добавлении работодателя в адресную книгу (триггер «добавлен контакт»). Код приложения не задействован.
* Содержание по §14 ТЗ: персонализированное приветствие, блок выбранных стримов, 3-шаговая механика работы сервиса, блок пробного периода (1 месяц).
* Мёрж-теги SendPulse: `{{Имя}}` (имя работодателя) и `{{Streams}}` (список стримов через запятую) — соответствуют переменным, которые `confirmEmployer()` передаёт при добавлении контакта в книгу.

---

==== ✅ TASK-DB-1: Инфраструктура Neon + Drizzle

*Выполнено:*

* Neon подключён через Vercel Marketplace; переменные `DATABASE_URL`, `DATABASE_URL_UNPOOLED` проставлены в Vercel и добавлены в `.env`.
* Зависимости: `drizzle-orm`, `@neondatabase/serverless` (runtime); `drizzle-kit`, `dotenv`, `ws` (dev).
* `lib/db/schema.ts` — схема таблицы `contactRequests` (10 колонок camelCase, без underscores).
* `lib/db/index.ts` — клиент `neon()` + `drizzle()` (neon-http driver, HTTP вместо WebSocket).
* `scripts/migrate.mjs` — скрипт миграции через `drizzle-orm/neon-http/migrator` (HTTP, не WebSocket). Загружает `.env.local` и `.env` через dotenv. Запуск: `npm run db:migrate`.
* `drizzle.config.ts` — конфигурация drizzle-kit для генерации миграций (`db:generate`).
* Миграция `lib/db/migrations/0000_high_wraith.sql` сгенерирована и применена к Neon.

NOTE: ℹ️ Drizzle выбран вместо Prisma — меньше бандл, быстрее cold start на Vercel serverless, нативная интеграция Neon через HTTP без постоянного соединения. Миграция применяется через `scripts/migrate.mjs` (HTTP-driver, не drizzle-kit migrate — последний зависал из-за WebSocket в Node.js).

*Фикс для Windows:* `npm run db:migrate` был вида `NODE_TLS_REJECT_UNAUTHORIZED=0 node scripts/migrate.mjs` — bash-синтаксис задания переменной окружения, который не работает под `cmd.exe` (дефолтный shell для npm-скриптов на Windows) — скрипт падал с `'NODE_TLS_REJECT_UNAUTHORIZED' is not recognized...` ещё до того, как успевал прочитать `.env.local`. По итогам обсуждения обход TLS-верификации убран полностью (а не просто перенесён в скрипт) — из `lib/db/index.ts`, `app/api/resume/route.ts` и из `package.json`; `db:migrate` теперь просто `node scripts/migrate.mjs`, кросс-платформенно.

---

==== ✅ TASK-DB-2: Перенос Contact Requests в Neon

*Выполнено:*

* Создан `lib/db/contact-requests.ts` — типы `ContactRequest`, `ContactRequestStatus` и три функции через Drizzle:
** `appendContactRequest()` → `db.insert(contactRequests).values(...)`
** `getContactRequests()` → `db.select().from(contactRequests).orderBy(desc(contactRequests.timestamp))`
** `updateContactRequestStatus(id, status)` → `db.update(...).set({ status }).where(eq(contactRequests.id, id))`
* `lib/sheets.ts` — удалены `appendContactRequest`, `getContactRequests`, `updateContactRequestStatus`, `ContactRequest`, `ContactRequestStatus`, `appendRow` (больше нигде не использовалась).
* `app/actions.ts` — обновлены импорты; `setContactRequestStatus(rowIndex: number, status)` → `setContactRequestStatus(id: string, status)`.
* `components/contact-requests-section.tsx` — `req.rowIndex` → `req.id`, импорт типов из `lib/db/contact-requests`.
* `app/editor/requests/page.tsx` — `getContactRequests` из `lib/db/contact-requests`, `getProfiles` по-прежнему из `lib/sheets`.
* Исторические данные в листе «Contact Requests» остаются как архив; новые запросы накапливаются в Neon.

*Схема на момент этой задачи (`lib/db/schema.ts`):*
[source,ts]
----
export const contactRequests = pgTable("contactRequests", {
  id:            text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  timestamp:     timestamp("timestamp").defaultNow().notNull(),
  listId:        text("listId").notNull().default(""),
  stream:        text("stream").notNull().default(""),
  candidateId:   text("candidateId").notNull().default(""),
  employerToken: text("employerToken").notNull().default(""),
  employerName:  text("employerName").notNull().default(""),
  company:       text("company").notNull().default(""),
  employerEmail: text("employerEmail").notNull().default(""),
  status:        text("status").notNull().default("Новый запрос"),
})
----

`candidateId` и `employerToken` — строки без FK: работодатели на тот момент ещё оставались в Sheets. Пустой `candidateId` = общий запрос. Функции — в `lib/db/contact-requests.ts`. `updateContactRequestStatus` принимает `id: string` (UUID), не `rowIndex`.

NOTE: ℹ️ Схема выше — историческая. После TASK-DB-3 (работодатели → Neon) поля `employerName`/`company`/`employerEmail`/`stream` убраны как дублирующие данные, доступные через join — см. <<✅ Нормализация contactRequests: FK на employers и streams вместо дублей>> ниже.

---

==== ✅ TASK-DB-8: Стримы — полный CRUD-редактор в Neon

*Выполнено:*

* `lib/db/schema.ts` — добавлена таблица `streams` (id, name, type, description).
* `lib/db/streams.ts` — `getStreams()`, `getStreamsDetailed()`, `createStreamRecord()`, `updateStreamRecord()`, `deleteStreamRecord()`.
* `app/actions.ts` — `createStream()`, `updateStream()`, `deleteStream()` (Server Actions с `revalidatePath`).
* `app/editor/streams/page.tsx` + `components/streams-table.tsx` — inline-редактирование, удаление (Trash2), добавление новой строки, dropdown типа (Industry / Functional), цветные бейджи.
* `components/editor-nav.tsx` — пункт «Стримы» (иконка Layers).
* `getStreams()` читает из Neon; лист «Streams» в Google Sheets более не используется.

*Актуальная схема (`lib/db/schema.ts`):*
[source,ts]
----
export const streams = pgTable("streams", {
  id:          serial("id").primaryKey(),
  name:        text("name").notNull().unique(),
  type:        text("type").notNull().default(""),
  description: text("description").notNull().default(""),
})
----

Миграция `lib/db/migrations/0001_real_wendell_vaughn.sql` создаёт таблицу и засевает 11 стримов.

NOTE: ℹ️ Задачи TASK-GC-1/2/3 (управление через Vercel Edge Config) рассматриваются отдельно как опциональная оптимизация — см. <<🔵 Опционально — Vercel Global Config для стримов>>.

---

==== ✅ TASK-24: Гео-фильтрация кандидатов в персонализированной подборке

*Выполнено:*

* `filterCandidatesForEmployer()` (`lib/sheets.ts`) расширена гео-фильтром: кандидат проходит, если его `countryPrimary` ИЛИ `countryDesired` входит в `[employer.country, ...employer.additionalCountries]` (сравнение без учёта регистра).
* Если `additionalCountries` содержит «Любая» → гео-фильтр не применяется (работодатель принимает любую страну).
* Кандидаты с пустыми `countryPrimary` и `countryDesired` — отфильтровываются (старые профили содержат только тестовые данные).
* Кейс `?secret=` (редактор) — фильтр не применяется, как и раньше: вызов `filterCandidatesForEmployer` обёрнут в `if (!isEditor && employerToken)` в `app/list/[listId]/page.tsx`.

NOTE: ℹ️ Закрывает только фильтрацию на странице подборки — сама рассылка всё ещё уходит без учёта страны получателя, см. TASK-25 в <<Активные задачи (требуется сделать)>>.

---

==== ✅ TASK-SP-1: LinkedIn и Telegram работодателя как переменные SendPulse

*Выполнено (было реализовано в рамках TASK-09/13):*

`confirmEmployer()` (`app/actions.ts`) передаёт оба поля при добавлении контакта в SendPulse:

[source,ts]
----
...(employer.telegram && { Telegram: employer.telegram }),
...(employer.linkedin && { LinkedIn: employer.linkedin }),
----

Переменные `{{Telegram}}` и `{{LinkedIn}}` доступны как мёрж-теги в шаблонах SendPulse.

---

==== ✅ TASK-26: Нейтральная формулировка вместо счётчика кандидатов в письме

*Выполнено:*

* `buildEmailHtml()` (`app/actions.ts`) больше не принимает `count` и не показывает точное число кандидатов — строка с датой заменена на нейтральную формулировку: `${date} · новые кандидаты в этом выпуске`.
* Вызов в `publishMailingList()` обновлён: `buildEmailHtml(list.stream, list.date, listUrl)` (без `list.entries.length`).
* Функция `candidatePlural()` (склонение числительного) удалена как более не используемая.

*Почему не стали ждать TASK-25:* переход на Unisender не подтверждён, аккаунт не куплен, сроки неизвестны — точный счётчик по гео-сегменту откладывается до реализации TASK-25 (если/когда она случится). Нейтральная формулировка закрывает разрыв доверия («в письме указано больше кандидатов, чем видно на странице после гео-фильтра TASK-24») уже сейчас, независимо от этого решения.

---

==== ✅ TASK-27: Автоматический маппинг тегов кандидата → стримы

*Выполнено:*

* `candidateMatchesStream(candidate, stream)` (`lib/sheets.ts`) — чистая функция сопоставления: кандидат попадает в стрим, если его `Profile.stream` (multi-select колонка «Stream») содержит название стрима, без учёта регистра.
* `getCandidatesForStream(candidates, stream)` (`lib/sheets.ts`) — фильтрует переданный массив кандидатов по `candidateMatchesStream`. Сигнатура берёт уже загруженный массив `Profile[]`, а не имя стрима строкой (как было в первоначальной формулировке задачи) — по аналогии с `filterCandidatesForEmployer(candidates, employer)`, чтобы не делать по отдельному запросу к Google Sheets API на каждый стрим при подсчёте для всей таблицы.
* `app/editor/streams/page.tsx` — параллельно с `getStreamsDetailed()` загружает `getProfiles()` (уже фильтрует «Активный»/legacy-пустой статус) и считает `candidateCounts` на стрим.
* `components/streams-table.tsx` — новая колонка «Кандидаты»: зелёный бейдж с числом подходящих активных кандидатов, серый «0» если совпадений нет; в режиме редактирования и в строке добавления нового стрима — те же данные/заглушка «—».

NOTE: ℹ️ Пункт «показывать в `/editor/candidates`» из исходной формулировки задачи не сделан — счётчик значим только в контексте стрима, поэтому достаточно `/editor/streams`.

*История решения:* первая версия (см. коммит до этого) сравнивала `Profile.industry`/`Profile.func` (TASK-04) со `stream.name` в зависимости от `type` стрима — по задумке ТЗ. На практике эти колонки добавлены «на будущее» и пока ни у кого не заполнены — все 11 стримов на реальных данных показывали 0 совпадений. Колонка `Stream` — то, что редактор реально проставляет кандидатам сегодня, поэтому матчинг переключён на неё; на реальных данных счётчики сразу стали ненулевыми (проверено на деве).

*Заодно:* единственное оставшееся место, где читались `profile.industry`/`profile.func`, — теги-бейджи на карточке кандидата в `/list/[listId]` (`app/list/[listId]/page.tsx`). Теперь они выводятся из того же источника, что и матчинг: `profile.stream` разбивается по `type` соответствующих стримов (`getStreamsDetailed()` из Neon) — Industry-стримы кандидата показываются как один набор тегов, Functional — как другой; `level` остаётся отдельным полем, как и раньше (со стримом не связан). Поля `industry`/`func` в `Profile`, их колонки в Sheets и `HEADER_ALIASES` не удалены — остаются как задел под §5 ТЗ, но сейчас нигде не читаются.

*Цвета тегов на карточке (доп. правка):* уровень — отдельный бейдж рядом с должностью (тёплый teal, не совпадает по цвету ни с одной категорией тегов), industry-теги — синие, functional — фиолетовые; та же пара цветов, что в `/editor/streams` (`TYPE_STYLE` в `streams-table.tsx`) — единая цветовая система тегов между редактором и публичной страницей.

---

==== ✅ TASK-30: Фильтры вместо секций в /editor/candidates и /editor/employers

*Выполнено:*

*Почему:* при росте числа кандидатов/работодателей три коллапсируемые секции по статусу (На проверке/Активные/Отклонённые и т. п.) становятся неудобны — нужно быстро находить записи сразу по нескольким признакам, а не листать секции.

* `lib/sheets.ts` — `Candidate` дополнен `stream`/`level`/`countryPrimary`/`countryDesired` (см. TASK-23 ниже) — нужны и для формы редактирования, и для фильтра.
* `app/editor/candidates/page.tsx`, `app/editor/employers/page.tsx` — вместо разбивки на pending/active/rejected на сервере передают весь массив + канонический список стримов (`getStreams()`) в клиентский компонент.
* `components/candidate-section.tsx` — коллапсируемые секции заменены панелью фильтров: текстовый поиск (имя/email/телефон), статус (по умолчанию **все статусы**), стрим, уровень. Фильтрация через `useMemo`, комбинируются одновременно (AND).
* `components/employer-section.tsx` — та же схема: поиск (имя/компания/email), статус (по умолчанию все), страна, стрим.
* Actions (Добавить/Отклонить/Редактировать, Подтвердить/Отклонить) не изменены — те же Server Actions, что и раньше.
* Добавлены цветные бейджи статуса/уровня/стримов прямо в строке — раньше эту информацию частично передавал заголовок секции, при плоском списке без секций её нужно было явно показать на уровне строки.

NOTE: ℹ️ Фильтры сознательно не персистятся в URL — состояние страницы (`useState`), сбрасываются при перезагрузке. Решение принято осознанно: проще в реализации, не нужно аккуратно сохранять `?secret=` при каждом изменении фильтра.

---

==== ✅ TASK-31: Редактирование данных работодателя

*Выполнено:*

*Что сейчас было до этой задачи:* в отличие от кандидата (TASK-23 ✅), у работодателя не было формы редактирования в приложении — только Подтвердить/Отклонить. Правки данных работодателя (email, стримы, страна и т. д.) делались только напрямую в Google Sheets.

* `lib/sheets.ts` — `updateEmployerFields(rowIndex, data)`: тот же паттерн, что `updateCandidateFields`, но с префиксом `'Employers'!` в диапазонах Sheets API. Поля вроде `Primary Contact`/`Additional Countries` не резолвятся через общий `mapHeader()`/`HEADER_ALIASES` (та таблица заточена под кандидатов/`Profile`) — добавлен локальный `EMPLOYER_FIELD_HEADER_ALIASES` (camelCase-имя поля → точный заголовок колонки).
* `app/actions.ts` — `updateEmployer(rowIndex, data)`: та же валидация, что у `registerEmployer()` (имя/email/телефон обязательны, хотя бы один стрим, Telegram/LinkedIn обязательны при соответствующем способе связи), пишет через `updateEmployerFields()`, `revalidatePath("/editor/employers")`.
* `components/employer-edit-modal.tsx` (новый файл) — форма как `EmployerRegistrationModal`, но без чекбокса согласия на обработку персональных данных (согласие уже дано при регистрации). Открывается по кнопке-карандашу в `EmployerRow`, как у кандидатов.
* `components/employer-registration-modal.tsx` — константы `CONTACT_OPTIONS`/`MAX_STREAMS`/`PRIMARY_COUNTRIES`/`ADDITIONAL_COUNTRIES`/`ANY_COUNTRY` экспортированы и переиспользуются в `employer-edit-modal.tsx` вместо дублирования.

*Донаполнение (после первой версии):* правки в `EmployerEditModal` не переотправляли данные в SendPulse — синхронизация происходила только один раз, при `confirmEmployer()`. Закрыто:

* `app/actions.ts` — `addToSendPulse()` переписана в `syncEmployerToSendPulse(employer, previous?)`. Без `previous` (первое подтверждение, вызывается из `confirmEmployer()`) ведёт себя как раньше. С `previous` (переданными старыми email/стримами, вызывается из `updateEmployer()` — только если `existing.status === "Подтверждён"`) — сначала удаляет устаревшее членство: если изменился email, старый адрес убирается из всех книг (мастер-книга + все прежние стримы), иначе — только из книг тех стримов, которые сняли в редактировании; затем как обычно добавляет/обновляет текущий email во всех актуальных книгах (мастер-книга + текущие стримы) с обновлёнными переменными (имя, телефон, Telegram/LinkedIn, Primary Contact, employer_token).
* `lib/sendpulse.ts` — добавлены `spDelete()` (DELETE-обёртка, как `spPost`/`spGet`) и `getBookId(name, token)` — поиск id книги по кэшу без автосоздания (в отличие от `getOrCreateBook`), используется для удаления: если книги для устаревшего стрима не существует, удалять просто нечего.
* `updateEmployer()` подтягивает текущую запись через `getEmployers().find(rowIndex)` до записи в Sheets — из неё берутся старые email/стримы/token и статус (чтобы не трогать SendPulse у ещё не подтверждённых заявок).

*Донаполнение 2 — отключение и удаление уже подтверждённого работодателя:* раньше единственный способ снять уже подтверждённого работодателя с рассылки или убрать его из таблицы — вручную в Google Sheets и вручную в SendPulse. Закрыто:

* `app/actions.ts` — `removeEmployerFromSendPulse(employer)`: полностью отписывает email от мастер-книги и всех книг его текущих стримов (в отличие от точечного удаления в `syncEmployerToSendPulse`, здесь всегда убираются все книги — работодатель больше нигде не должен остаться).
* `rejectEmployer(rowIndex)` расширена: если у записи `status === "Подтверждён"`, перед сменой статуса на «Отклонён» вызывает `removeEmployerFromSendPulse()`. Для заявок «На проверке» (никогда не попадавших в SendPulse) поведение не изменилось.
* `deleteEmployer(rowIndex)` (новый экшен) — та же отписка от SendPulse при `status === "Подтверждён"`, затем `deleteEmployerRow(rowIndex)` физически удаляет строку из таблицы.
* `lib/sheets.ts` — `deleteEmployerRow(rowIndex)`: находит числовой `sheetId` (gid) листа «Employers» через метаданные таблицы, затем `batchUpdate` с `deleteDimension` (ROWS, `startIndex: rowIndex - 1, endIndex: rowIndex`) — физическое удаление строки, а не просто очистка значений.
* `components/employer-section.tsx` — кнопки не в форме редактирования, а прямо в строке таблицы `EmployerRow`, тем же паттерном, что уже был у «Подтвердить»/«Отклонить» для заявок «На проверке»: для `status === "Подтверждён"` появляется «Отключить», для `status === "Отклонён"` — снова «Подтвердить» (возврат в рассылку), значок корзины (`Trash2`) для удаления виден всегда. Клик по корзине не удаляет сразу — переключает область действий строки в отдельный шаг подтверждения («Вы уверены, что хотите удалить работодателя?» + «Да, удалить»/«Отмена»), а не браузерный `window.confirm()`. `handleReject(result)` в `EmployerRow` переиспользуется и для «Отклонить» (На проверке), и для «Отключить» (Подтверждён) — оба вызывают один и тот же `rejectEmployer()`, серверная логика уже сама решает, трогать ли SendPulse, по текущему статусу записи. Повторное «Подтвердить» у отклонённого работодателя переиспользует тот же `confirmEmployer()`, что и первое подтверждение заявки — он безусловно (пере)синхронизирует SendPulse и ставит статус «Подтверждён» независимо от того, каким был статус до этого, так что отдельного экшена для «вернуть в рассылку» не потребовалось.

---

==== ✅ TASK-32: История версий резюме в Neon

*Что сейчас было до этой задачи:* когда рекрутер загружал в `CandidateEditModal` новый файл резюме вместо старого (например, после того как кандидату помогли подправить или брендировать резюме), в Sheets просто перезаписывался URL — старый blob-файл в Vercel Blob не удалялся и никуда не девался, но и ссылки на него больше нигде не было: чистый мусор, накапливающийся в хранилище без возможности его найти или использовать.

*Решение:* вместо удаления старых файлов — хранить полную историю версий резюме кандидата отдельно, в Neon, и показывать её в редакторе. Google Sheets по-прежнему хранит только URL последней версии (используется на публичных страницах, в проксировании и т. д.) — ничего в этом контуре не поменялось.

* `lib/db/schema.ts` — новая таблица `candidateResumes` (`id` text/UUID, `candidateId` text, `kind` text — `"file"`/`"link"`, `filename` text, `url` text, `createdAt` timestamp). Миграция `lib/db/migrations/0002_harsh_tomorrow_man.sql`, применена к продовой Neon.
* `lib/db/resumes.ts` (новый файл) — `addResumeVersion(data)` (не пишет, если `candidateId` или `url` пустые — молча пропускает, не бросает) и `getResumeVersions(candidateId)` (новые сверху, `orderBy(desc(createdAt))`). Если `filename` не передан явно и `kind === "file"` — имя выводится из URL через `blobFilenameFromUrl()` (ниже), а не остаётся пустым; для `"link"` фолбэка нет — у ссылки нет «имени файла».
* `lib/sheets.ts` — `Candidate` дополнен `id: string` (то же самое поле `id`/UUID, что уже резолвилось через `HEADER_ALIASES` для `Profile`, просто не попадало в объект `Candidate`, используемый редактором) — нужен как стабильный ключ для истории версий в Neon.
* `lib/blob.ts` (новый файл) — `isOwnFileUrl(url)`: `true`, если URL указывает на собственный Vercel Blob — напрямую (хост оканчивается на `blob.vercel-storage.com`) или через свой прокси `/api/resume?url=<blobUrl>` (тот же паттерн, что уже валидировал `app/api/resume/route.ts` — константа `BLOB_HOST` вынесена сюда и переиспользована оттуда, вместо дублирования). Любой другой URL — «ссылка», введённая кандидатом/рекрутёром вручную. Там же `blobFilenameFromUrl(url)` — берёт последний сегмент пути (`resumes/1754460447259-a1b2c3d4.pdf` → `1754460447259-a1b2c3d4.pdf`); это сгенерированное имя файла в Blob, а не оригинальное имя, под которым кандидат загружал файл — Vercel Blob оригинальное имя нигде не хранит, `put()` вызывается уже с нашим сгенерированным путём.
* `app/actions.ts`:
** `registerCandidate()` — генерирует `id` (как и раньше, `crypto.randomUUID()`, не через внешний скрипт), после успешной записи строки сразу пишет первую версию резюме в `candidateResumes`, если `resumeUrl` не пустой. Благодаря синхронной генерации `id` в этой же функции первый файл кандидата тоже попадает в историю — не только последующие замены через редактор.
** `updateCandidate()` дополнен `candidateId` (обязателен), `resumeVersionChanged?: boolean` и `resumeFilename?: string` — если `resumeVersionChanged` и `resumeUrl` не пустой, пишет новую версию в `candidateResumes` после записи в Sheets. Клиент передаёт `resumeVersionChanged` только когда итоговый `resumeUrl` реально отличается от `candidate.resumeUrl` на момент открытия формы — редактирование прочих полей без изменения резюме новую версию не создаёт.
** `kind` (`"file"` / `"link"`) в обоих случаях вычисляется на сервере через `isOwnFileUrl(resumeUrl)`, а не берётся из клиента — клиент присылает только `resumeFilename` (для файла) и признак изменения; какой именно вкладкой пользовался человек в форме, на классификацию не влияет.
** `getCandidateResumeHistory(candidateId)` — новый экшен, отдаёт историю версий для конкретного кандидата.
* `components/candidate-registration-modal.tsx` / `components/candidate-edit-modal.tsx` — передают только `resumeFilename` (оригинальное имя загруженного файла, только когда реально был выбран новый файл); тип резюме на клиенте больше не вычисляется и не передаётся.
* `components/candidate-edit-modal.tsx` — под блоком «Резюме» новый блок с историей: подгружается через `getCandidateResumeHistory(candidate.id)` в `useEffect` при открытии модалки, разбит на две подгруппы — «Файлы (N)» и «Ссылки (N)» (только непустые), в каждой — список с датой и кнопкой «Открыть» (та же прокси-ссылка `/api/resume`, что уже используется для текущего резюме). Пустое состояние — «История резюме появится здесь после следующей загрузки нового файла или ссылки».
* `scripts/backfill-resume-history.ts` — одноразовый скрипт для кандидатов, заведённых до этой задачи: читает текущих кандидатов из Sheets, для каждого с непустыми `id` и `resumeUrl` пишет одну запись в `candidateResumes` (`kind` — тем же `isOwnFileUrl()`, что и в боевом коде; дата — исходный `Timestamp` кандидата, а не «сегодня», через новый параметр `addResumeVersion({ createdAt })`). Идемпотентен — повторный запуск пропускает уже записанные пары `(candidateId, url)`. Запускается через `NODE_OPTIONS="--conditions=react-server" npx tsx scripts/backfill-resume-history.ts` — импортирует `lib/sheets.ts`/`lib/db/resumes.ts` напрямую (`import "server-only"` иначе бросает исключение вне сборки Next). Прогнан один раз против прод. Sheets/Neon: 87 кандидатов → 77 записей (71 файл, 6 ссылок), 10 без резюме, 0 без id.

*Осознанный трейд-офф:* старые blob-файлы теперь никогда не удаляются — они все остаются в Vercel Blob и все видны в списке версий. Место в Blob растёт с каждой заменой резюме, но взамен ничего не теряется и не требует отдельной политики очистки — сознательный выбор в пользу полноты истории, а не экономии места.

*Не реализовано:* кандидаты, у которых нет `id` (устаревшие строки, вписанные в таблицу вручную до того, как проставился ID) — история версий для них не пишется и не отображается, поле просто не показывается в модалке (`candidate.id` пустой). Восстановления/выбора конкретной старой версии как «текущей» из списка нет — список только для просмотра и скачивания, чтобы вернуть старый файл текущим, нужно перезалить его вручную.

**Донаполнение — история резюме прямо в таблице `/editor/candidates`:** раньше файлы/ссылки были видны только внутри формы редактирования — приходилось открывать карточку каждого кандидата, чтобы узнать, есть ли у него резюме.

* `lib/db/resumes.ts` — `getResumeVersionsForCandidates(candidateIds)`: один запрос (`WHERE candidateId IN (...)`) вместо N отдельных — результат сгруппирован по `candidateId` (объект `Record<string, ResumeVersion[]>`), каждая группа уже отсортирована новыми сверху, т. к. общий `orderBy(desc(createdAt))` сохраняет относительный порядок внутри групп.
* `app/editor/candidates/page.tsx` — вызывает `getResumeVersionsForCandidates()` один раз для всех кандидатов страницы и передаёт карту вниз в `CandidateSection` — без этого список из ~90 кандидатов означал бы ~90 отдельных запросов к Neon при открытии страницы.
* `components/candidate-section.tsx` — таблица `CandidateRow` (было — список карточек `<div>`) с колонками Кандидат / Контакты / Файлы / Ссылки / Дата регистрации / Активен с / Действия. Колонки «Файлы» и «Ссылки» показывают до 3 последних версий каждого типа как компактные чипы-ссылки (иконка + имя файла или домен ссылки, обрезанные `truncate`) — ведут прямо на файл/ссылку, открывать карточку кандидата для этого больше не нужно. Колонка «Дата регистрации» — `candidate.timestamp`, перед «Активен с».

---

==== ✅ TASK-33: Отключение/удаление кандидата и быстрое добавление из редактора

*По аналогии с TASK-31/донаполнениями для работодателя* — тот же набор действий прямо в строке таблицы `/editor/candidates`, что уже был сделан для `/editor/employers`.

**Отключение и повторное подтверждение:**

* Кандидаты не участвуют в SendPulse (регистрация явно это не использует) — в отличие от работодателей, здесь не нужен отдельный шаг отписки. `rejectCandidate(rowIndex)` и `approveCandidate(rowIndex)` уже существовали и не требовали изменений — переиспользованы как есть.
* `components/candidate-section.tsx` — для `status === "Активный"` в строке появляется «Отключить» (вызывает `rejectCandidate`), для `status === "Отклонён"` — снова «Подтвердить» (вызывает `approveCandidate`, обновляет `activeSince` на сегодня, как и при первом одобрении). `handleReject(result)` переиспользуется и для «Отклонить» (На проверке), и для «Отключить» (Активный) — тот же паттерн, что уже был у работодателя.

**Удаление — с очисткой Neon и Blob:**

* `lib/blob.ts` — `isOwnFileUrl()`/`blobFilenameFromUrl()` рефакторены поверх общей `resolveBlobUrl(url)`: возвращает настоящий `blob.vercel-storage.com` URL (напрямую или размотанный из `/api/resume?url=` прокси) либо `null`, если ссылка не наша — переиспользуется теперь и для определения типа, и для самого удаления файла.
* `lib/db/resumes.ts` — `deleteResumeVersions(candidateId)`: удаляет все строки истории резюме кандидата из `candidateResumes`.
* `lib/sheets.ts` — `appendCandidateRow()` теперь возвращает `{ rowIndex }` (парсится из `updates.updatedRange` в ответе Sheets API `values:append`) — нужен для донаполнения только что созданной строки через `updateCandidateFields()` (см. быстрое добавление ниже). `deleteCandidateRow(rowIndex)` — тот же паттерн `deleteDimension`, что у `deleteEmployerRow()`, но целится в **первый лист** таблицы (`meta.sheets[0]`), а не по названию — у кандидатов, в отличие от «Employers», диапазоны в коде (`SHEET_RANGE = "A1:Z1000"`) нигде не квалифицированы именем листа.
* `app/actions.ts` — `deleteCandidate(rowIndex, candidateId)`: сначала читает всю историю резюме кандидата (`getResumeVersions`), для каждой версии, если это файл на своём Blob (`resolveBlobUrl()` не `null`), зовёт `del()` из `@vercel/blob` — каждый вызов обёрнут в `.catch()` с `console.warn`, чтобы неудачное удаление одного файла (например, уже отсутствующего) не блокировало остальную очистку. Затем `deleteResumeVersions()` и `deleteCandidateRow()`.
* `components/candidate-section.tsx` — значок корзины виден всегда, переключает область действий строки в отдельный шаг подтверждения («Удалить кандидата?» + «Да, удалить»/«Отмена»), тот же паттерн, что у работодателя.

**Быстрое добавление кандидата из редактора:**

* `app/actions.ts` — `createCandidate(data)`: генерирует `id` синхронно (`crypto.randomUUID()`, как `registerCandidate()`), пишет через `appendCandidateRow()` только проверенный безопасный набор полей (`id`, `timestamp`, `name`, `email`, `phone`, `"resume url"`, `"cover letter"`, `status: "На проверке"` — те же ключи, что уже надёжно резолвятся в `appendCandidateRow()`), затем сразу `updateCandidateFields(rowIndex, {...})` для `title`/`level`/`activeSince`/`stream`/`countryPrimary`/`countryDesired`/`summary` — то есть через ту же функцию, что уже надёжно резолвит любые названия колонок (`mapHeader()`) независимо от языка заголовков в реальной таблице, вместо того чтобы гадать формат ключей для `appendCandidateRow()`. Статус остаётся «На проверке», как и у публичной регистрации, а не «Активный» — заносить кандидата в редакторе может один человек (например, секретарь), а проверять — другой (рекрутёр); быстрое добавление ускоряет только ввод данных, не подменяет модерацию.
* `components/candidate-create-modal.tsx` (новый файл) — форма как у `CandidateEditModal`, но без истории резюме и без «текущего резюме» (нечего показывать у нового кандидата); «Активен с» предзаполнено сегодняшней датой. Открывается по кнопке «+ Добавить кандидата» в панели фильтров `CandidateSection`.

**Побочная находка — токен `destructive` нигде не был определён:** в процессе визуальной проверки (скриншоты через Playwright) обнаружилось, что кнопка «Да, удалить» отрисовывалась видимой, но без текста — `bg-destructive`/`text-destructive`/`border-destructive` использовались по всему приложению (включая уже существующие кнопки «Отклонить» у кандидатов и работодателей), но соответствующий цвет ни разу не был объявлен в `app/globals.css` — Tailwind v4 молча не генерировал для них никакого CSS. Добавлены `--destructive`/`--destructive-foreground` (oklch, в том же стиле, что остальные токены) и их `--color-*` алиасы в `@theme inline` — красный теперь везде, где и задумывался: кнопки «Отклонить»/«Отключить»/«Удалить», блоки ошибок.

---

==== ✅ Страницы редактора на всю ширину; убрана кнопка миграции колонок; исправлена подсказка в «Запросах»

* Все пять страниц `/editor/*` (Выпуски, Работодатели, Кандидаты, Запросы, Стримы, Настройки) были в узком контейнере (`max-w-3xl`/`max-w-4xl`/`max-w-5xl`) — убрано, теперь на всю ширину `main`, как уже было у `/editor/candidates`.
* `/editor/requests` — подсказка ошибочно утверждала, что смена статуса запроса сохраняется «в Google Sheets»; на самом деле Contact Requests перенесены в Neon ещё в TASK-DB-2. Текст исправлен на нейтральное «в базе данных» — не называет Neon по имени, это подсказка для нетехнического пользователя, а не техническая документация.
* `/editor/settings` — раздел «Google Sheets» (кнопка «Проверить или добавить колонки в таблицу») убран полностью: он покрывал разовую миграцию колонок для полей, добавленных уже после того, как боевая таблица была создана (Level/Industry/Function/CountryPrimary/CountryDesired/Summary/ExcludedCompanies/ExcludedIndustries для кандидатов, Country/Additional Countries для работодателей) — все эти колонки в боевой таблице уже есть, новых полей, требующих такой миграции, не планируется. Удалены как полностью переставшие использоваться: `components/add-columns-button.tsx`, экшены `addProfileColumns()`/`addAllColumns()` (`app/actions.ts`), функции `ensureProfileColumns()`/`ensureEmployerColumns()` (`lib/sheets.ts`). Страница теперь пустая заглушка «Пока здесь нечего настраивать» — задел на будущее, если появятся настоящие настройки.
* `ensureCandidateColumns()` **не удалена** — в отличие от двух других, она вызывается не только вручную из Настроек, но и автоматически внутри `appendCandidateRow()` при каждой новой регистрации/быстром добавлении кандидата (подстраховка на случай отсутствующих колонок) — это боевой путь записи, а не разовая миграция.

---

==== ✅ TASK-DB-3: Employers → Neon

*Почему первый кандидат на перенос:* блокер («сначала полный UI-редактор, потом перенос хранения») снят полностью — `EmployerEditModal` (TASK-31) покрывает все поля, а весь жизненный цикл статуса (Подтвердить/Отключить/Отклонить/Удалить/Подтвердить заново) теперь тоже целиком через UI (TASK-31-доп./TASK-33-парность) — прямого захода в Google Sheets не требуется ни для одного поля. У Candidates та же миграция ещё преждевременна (несколько полей до сих пор правятся только в Sheets), у Mailing Lists — упирается в ещё не построенный конструктор выпусков (TASK-29).

* `lib/db/schema.ts` — новая таблица `employers` (`token` text PK, `$defaultFn(() => crypto.randomUUID())` — тот же UUID, что раньше был колонкой `ID`, он же `employer_token` в SendPulse и `?e=` в публичной ссылке; `streams`/`additionalCountries` — нативные Postgres `text[]`, а не comma-joined строка, как было в Sheets, — реальное упрощение: убирает `.join(", ")`/`parseMultiValue()` на всех вызывающих сторонах). Миграция `lib/db/migrations/0003_redundant_chat.sql`, применена к продовой Neon.
* `lib/db/employers.ts` (новый файл) — тот же паттерн, что `lib/db/streams.ts`/`contact-requests.ts` (`import "server-only"`, плоские async-функции, никаких классов): `getEmployers()`, `getEmployerByToken(token)` (теперь честный `WHERE token = ...` вместо «выгрузить всех и найти в JS», как было раньше), `getEmployersByStream(stream)` (перенесена 1-в-1 для паритета — вызывающих в приложении сейчас нет), `createEmployer(data)`, `updateEmployerFields(token, data)` (объединяет прежние `updateEmployerStatus`+`updateEmployerFields` в одну функцию — в Sheets они были раздельными только из-за ячеечной записи, в Drizzle любое обновление — один `db.update(...).set(data)` независимо от набора полей), `deleteEmployer(token)`, `filterCandidatesForEmployer()` (чистая функция без I/O, перенесена вместе с типом `Employer`, который теперь ей и принадлежит).
* Идентификатор везде сменился с `rowIndex` (позиция строки в таблице — только в Sheets имела смысл) на `token` (тот же UUID, что уже был единственным стабильным ключом во всём приложении — просто теперь используется явно, а не как побочное поле).
* Бэкафилл (`scripts/backfill-employers-to-neon.ts`, одноразовый, удалён после использования — тот же паттерн, что `scripts/backfill-resume-history.ts` из TASK-32): пока старый Sheets-код ещё был на месте, прочитал все 14 строк из листа «Employers» и вставил в Neon с сохранением исходных `token`/`timestamp`/`status`. Лист «Employers» в Google Sheets **не удалён** — оставлен как архив, аналогично тому, как TASK-DB-2 оставил архивным лист «Contact Requests» после переноса в Neon.
* Переключены на новый модуль: `app/actions.ts` (`registerEmployer`/`updateEmployer`/`confirmEmployer`/`rejectEmployer`/`deleteEmployer` — параметр `rowIndex` → `token`; `syncEmployerToSendPulse`/`removeEmployerFromSendPulse` изменений не потребовали — они и раньше принимали только простые объекты `{name, email, ...}`, полностью независимые от способа хранения), `app/editor/employers/page.tsx`, `app/list/[listId]/page.tsx` (публичная персонализация по `?e=` токену — `getEmployerByToken`/`filterCandidatesForEmployer`, без изменения логики), `components/employer-section.tsx`, `components/employer-edit-modal.tsx` (только импорт типа и `employer.rowIndex` → `employer.token` в вызовах действий и React `key`).
* Удалён весь Sheets-код по работодателям из `lib/sheets.ts`: `Employer`, `EmployerStatus`, `parseEmployerRows`, `getEmployers`, `getEmployerByToken`, `getEmployersByStream`, `appendEmployerRow`, `updateEmployerStatus`, `updateEmployerFields` (employer-версия), `deleteEmployerRow`, `EMPLOYER_FIELD_HEADER_ALIASES`, `EMPLOYERS_HEADERS`, `filterCandidatesForEmployer` (перенесена) — подтверждено `grep`, что вне `lib/sheets.ts` ничего из этого больше не импортируется.
* Проверено вручную полным жизненным циклом на реальном тестовом работодателе через настоящий UI (регистрация → Подтвердить → редактирование через карандаш → Отключить → Подтвердить заново → Удалить), включая проверку публичной ссылки `/list/[listId]?e=<token>` для уже перенесённого работодателя — тестовые данные полностью удалены после проверки (0 остатков в Neon).

---

==== ✅ Реальный текст ошибки при регистрации/редактировании работодателя

*Проблема:* при регистрации работодателя с уже занятым email форма вместо понятного текста показывала общую ошибку Next.js: «An error occurred in the Server Components render. The specific message is omitted in production builds…». Реальное сообщение («Работодатель с таким email уже зарегистрирован») было видно только в серверных логах (`digest`).

*Причина:* осознанное поведение Next.js — в production-сборке Server Actions отдают клиенту общий текст для любого `throw new Error(...)`, чтобы не утекали детали непредвиденных ошибок. Это ломает ожидаемые, «бизнесовые» ошибки валидации (дубликат email/телефона, пустые обязательные поля), которые как раз нужно показывать пользователю как есть.

*Решение:* `registerEmployer()` и `updateEmployer()` (`app/actions.ts`) теперь возвращают `{ ok: true } | { ok: false, error: string }` вместо `throw` для ожидаемых случаев валидации. `EmployerRegistrationModal`/`EmployerEditModal` читают `result.error` из возврата; `catch` оставлен только на непредвиденные сбои (сеть/БД) — там по-прежнему общий фолбэк-текст, что корректно.

NOTE: ℹ️ Тот же паттерн — `throw new Error()` для валидации ломает вывод сообщения в production — остаётся в других Server Actions (`registerCandidate`, `createCandidate` и т. д.), но там соответствующие ошибки либо уже гасятся клиентской валидацией (`required`-поля), либо не встречались на практике. Исправлено точечно там, где реально ломалось (дубликат email/телефона у работодателя).

---

==== ✅ Нормализация contactRequests: FK на employers и streams вместо дублей

*Почему актуально после TASK-DB-3:* пока работодатели жили в Sheets, `contactRequests` хранил снимок их данных (`employerName`/`company`/`employerEmail`) прямо в строке запроса — иначе было не получить эти поля при отображении. После переноса работодателей в Neon эти поля стали чистым дублированием: `employerToken` уже указывает на строку в `employers`, откуда те же данные можно получить join'ом.

* `lib/db/schema.ts` — из `contactRequests` убраны `employerName`, `company`, `employerEmail` (резолвятся через `LEFT JOIN`/`INNER JOIN` на `employers` по `employerToken`) и `stream` (заменён на `streamId` — `INTEGER`, FK на `streams.id`).
* `employerToken` — теперь `NOT NULL`, FK на `employers.token`, `ON DELETE CASCADE`: запрос без работодателя не имеет смысла, поэтому при удалении работодателя его запросы на контакт удаляются вместе с ним (осознанное решение, обсуждено отдельно — первая версия была `SET NULL`, но это оставляло бы «осиротевшие» строки без контекста).
* `streamId` — `ON DELETE SET NULL`: переименование/удаление стрима не должно стирать сам факт запроса.
* `lib/db/contact-requests.ts` — `getContactRequests()` делает `innerJoin(employers, ...)` + `leftJoin(streams, ...)`, отдаёт тот же публичный тип `ContactRequest` (с `stream`/`employerName`/`company`/`employerEmail` строками), что и раньше — UI (`contact-requests-section.tsx`, `/editor/requests`) не менялся.
* `lib/db/streams.ts` — добавлена `getStreamIdByName(name)`; `app/actions.ts` (`submitContactRequest`/`submitGeneralInquiry`) резолвит `streamId` перед записью вместо передачи имени стрима строкой.
* Миграция в два шага (`0004`/`0005`/`0006`) — интерактивный prompt `drizzle-kit generate` при одновременных add+drop колонок недоступен без TTY, поэтому add и drop разнесены по отдельным `db:generate`-вызовам. Прогнано на проде: 5 существующих строк, все успешно смигрированы (100% совпадение по имени стрима и по токену работодателя).

---

==== ✅ Кнопка «Добавить работодателя» в редакторе

По аналогии с быстрым добавлением кандидата (TASK-33 ✅) — работодателя теперь можно завести прямо из `/editor/employers`, без публичной формы на главной странице.

* `components/employer-create-modal.tsx` (новый файл) — форма в стиле `employer-edit-modal.tsx` (без чекбокса согласия), но вызывает существующий `registerEmployer()` целиком — та же валидация (обязательные поля, дубли email/телефона), тот же статус по умолчанию «На проверке». Новой бизнес-логики нет.
* `registerEmployer()` (`app/actions.ts`) дополнен `revalidatePath("/editor/employers")` в конце — раньше не требовалось (действие вызывалось только с публичной формы, вне контекста редактора).
* `components/employer-section.tsx` — убран ранний `return` на пустом списке работодателей: он скрывал панель фильтров вместе с новой кнопкой, так что при нуле работодателей добавить было бы неоткуда. Пустое состояние теперь текст внутри карточки списка.

---

==== ✅ Улучшения UI резюме: индикатор «+N», убран дубль «Текущее резюме», полная ширина, имя файла при скачивании

* Таблица кандидатов (`candidate-section.tsx`): колонки «Файлы»/«Ссылки» молча обрезали список до 3 чипов без какого-либо намёка, что их больше — добавлен чип «+N» (title — полный список остальных имён/ссылок).
* Форма редактирования (`candidate-edit-modal.tsx`): убран блок «Текущее резюме» над историей версий — дублировал самую свежую запись, которая и так видна и открывается в списке «Файлы»/«Ссылки» чуть ниже.
* Списки истории резюме были в два столбца рядом (`grid-cols-2`) — из-за узкой колонки обрезались длинные имена файлов и URL; стали последовательными секциями на всю ширину формы. Добавлен `title` с полным именем/ссылкой на усечённых `truncate`-строках.
* *Скачивание под настоящим именем файла:* `/api/resume` отдавал `Content-Disposition` с именем из пути blob-объекта (например `1789050579070-f807f231.docx`) — реальное имя, введённое кандидатом при загрузке, хранится отдельно в `candidateResumes.filename` и никак не участвовало в скачивании. Добавлен необязательный query-параметр `?filename=`; ссылки «Открыть» передают его через новый `withDownloadFilename()` (`lib/blob.ts`, только для `kind: "file"`) — кириллица идёт через `filename*=UTF-8''` (RFC 5987) с ASCII-фолбэком для старых клиентов. Без параметра — прежнее поведение (имя из пути), так что работает и для уже существующих ссылок в БД.

NOTE: ℹ️ Ссылки на резюме в БД абсолютные (`APP_URL/api/resume?...`) — эффект от параметра `?filename=` виден только после деплоя на тот же `APP_URL`, даже если тестировать локально: клик уходит на боевой `/api/resume`, а не на локальный dev-сервер (если `APP_URL` в `.env.local` указывает на прод, как это обычно и есть — отдельного стенда нет).

---

==== ✅ Удаление отдельных файлов/ссылок резюме из истории версий

У каждой версии резюме (Файлы/Ссылки) в `CandidateEditModal` теперь есть корзина — удаляет запись из `candidateResumes` (Neon) и, для файлов, сам файл из Vercel Blob (best-effort, тот же паттерн, что `deleteCandidate()`). Требует отдельного подтверждения в строке («Удалить? Да/Отмена»), без `window.confirm()`.

Версия, совпадающая с текущим `candidate.resumeUrl`, не удаляется — кнопка задизейблена с подсказкой «сначала загрузите новую версию»: иначе можно было бы удалить активное резюме и оставить в Sheets ссылку на уже стёртый файл. Новый экшен `deleteCandidateResumeVersion(candidateId, versionId)` (`app/actions.ts`) смотрит `kind`/`url` заново из Neon по `versionId`, а не доверяет значениям от клиента.

---

==== ✅ Дата регистрации работодателя в редакторе

`employers.timestamp` существовал с самого переноса в Neon (TASK-DB-3) и хранил настоящие даты у всех записей — просто никогда не выводился в `/editor/employers`. Добавлена строка «зарегистрирован {дата}» в `EmployerRow` (`components/employer-section.tsx`), тем же форматом, что «Дата регистрации» у кандидатов. Только вывод — без изменений в данных.

NOTE: ℹ️ Был запрос backfill'нуть существующие записи фиксированной датой — отклонён после проверки: у всех записей уже были настоящие разные даты регистрации, backfill стёр бы реальные данные без причины.

---

==== ✅ Убраны bio/website/location из кода

Поля существовали в `Profile` (`lib/sheets.ts`) и рендерились в `ProfileView` (`/profile/[id]`), но колонок `bio`/`website`/`location` никогда не было в боевой таблице «Candidates Database» — проверено прямым чтением заголовка (`getSheetTable()`). Поля были кодом без данных с самого начала.

* `lib/sheets.ts` — `bio`/`website`/`location` убраны из `Profile`, `KNOWN_KEYS`, `HEADER_ALIASES` (включая алиас `резюме` → `bio`) и `buildProfile()`.
* `components/profile-view.tsx` — убран блок bio, `website`/`location` из списка контактов (`Globe`/`MapPin` импорты тоже убраны — больше не используются).
* `app/profile/[id]/page.tsx` — `generateMetadata()` больше не пытается взять `description` из `profile.bio`.
* `app/page.tsx` — из подсказки «Рекомендуемые колонки» на экране первоначальной настройки убраны несуществующие поля.

Заодно (по отдельному запросу, до этой правки) из боевой таблицы вручную удалены три никем не читаемых столбца заметок — `Интервьюер`, `Баллы`, `Примечания` (проверено: `Баллы` был заполнен у 8/117 строк, остальные два — пустые везде; данные признаны ненужными и удалены осознанно). `docs/manager-playbook.adoc` обновлён — соответствующая строка убрана из таблицы полей.

---

==== ✅ Сортировка кандидатов по дате регистрации и «Активен с»

Заголовки «Дата регистрации»/«Активен с» в `/editor/candidates` (`components/candidate-section.tsx`) стали кликабельными: первый клик — по убыванию (новые сверху), второй — по возрастанию; значок показывает активное поле и текущее направление.

Два поля хранятся в разных форматах — нужны отдельные парсеры:

* `candidate.timestamp` — ISO (`new Date().toISOString()`), но может быть пустым на вручную заведённых строках (по мануалу — «можно оставить пустым»).
* `candidate.activeSince` — ru-RU текст (`"21.07.2026"`, не ISO — см. `toRuDate()`/`toISODate()` в `candidate-edit-modal.tsx`).

Строки без распознаваемой даты в выбранном поле сортируются в конец списка независимо от направления — не перемешиваются с настоящими датами. Сортировка — `useMemo` поверх уже отфильтрованного списка; счётчик и сами фильтры не затронуты.

---

==== ✅ «Активен с» заполняется автоматически только если было пусто

*Что было:* `approveCandidate()` (`app/actions.ts`) безусловно перезаписывал `activeSince` сегодняшней датой при *любом* подтверждении — включая повторное «Подтвердить» уже отключённого кандидата (TASK-33), стирая исходную дату активации.

*Стало:* `approveCandidate(rowIndex, currentActiveSince?)` получает текущее значение поля от клиента (`candidate-section.tsx` передаёт `candidate.activeSince`) и пишет сегодняшнюю дату только если оно пустое — иначе `updateCandidateStatus()` вызывается с `activeSince: undefined`, и колонка не трогается (в `lib/sheets.ts` запись в ячейку уже была условной на `!== undefined`, менять не пришлось).

Прогнано на реальных данных: регистрация через публичную форму (`activeSince` гарантированно пусто) → «Добавить» → колонка заполнилась сегодняшней датой → через карандаш выставлена дата `15.01.2020` → «Отключить» → «Подтвердить» → дата осталась `15.01.2020`, не сброшена на сегодня.

---

==== ✅ Счётчик подписчиков на странице стримов

Рядом со счётчиком подходящих кандидатов (TASK-27) в `/editor/streams` добавлена колонка «Подписчики» — число *подтверждённых* работодателей (`status === "Подтверждён"`), у которых этот стрим есть в списке `streams`. Заявки «На проверке» и отклонённые не считаются — только те, кто реально получает рассылку.

* `lib/db/employers.ts` — новая `confirmedEmployersForStream(employers, stream)`, чистая функция без I/O — мирроит `getCandidatesForStream()` (`lib/sheets.ts`, TASK-27): фильтрует уже загруженный массив, а не делает отдельный запрос на каждый стрим (в отличие от существующей `getEmployersByStream()`, которая как раз по одному запросу на стрим и остаётся неиспользуемой).
* `app/editor/streams/page.tsx` — один `getEmployers()` на всю страницу, счётчики считаются в памяти для всех 11 стримов.
* `components/streams-table.tsx` — новая колонка между «Кандидаты» и «Описание»; бейдж голубого цвета (`sky`), чтобы не путать с зелёным у кандидатов.

Прогнано на реальных данных: прямой запрос к БД (4 подтверждённых работодателя из 19 всего, все на стримах Tech/Executive) совпал с тем, что показывает таблица.

---

== Активные задачи (требуется сделать)

=== 🔴 Критично — гео-сегментация рассылки

NOTE: ℹ️ Гео-данные собираются формами (TASK-07/15, TASK-04 — оба ✅), фильтрация кандидатов на странице подборки реализована (TASK-24 ✅ — см. <<Выполнено>>). Остаётся один открытый разрыв: письмо кампании по-прежнему уходит всем подписчикам книги стрима без учёта страны работодателя — закрывает его TASK-25 ниже.

==== TASK-25: Гео-сегментация получателей рассылки (ждёт решения о переходе на Unisender)

*Что сейчас:* `publishMailingList()` (`app/actions.ts`) отправляет кампанию в SendPulse-книгу по имени стрима — вся книга получает одно письмо независимо от страны работодателя. TASK-24 ✅ закрыл фильтрацию на странице подборки: работодатель получает письмо, кликает, видит только релевантных кандидатов своей страны.

*Текущий компромисс:* приемлемо на этапе MVP с тестовыми данными. Работодатель с нулевым пересечением получит пустую страницу — это допустимо до появления реальных работодателей.

*Исследование завершено — все варианты SendPulse исчерпаны:*

[cols="2,1,3", options="header"]
|===
| Вариант | Результат | Причина

| Сегменты (бесплатный тариф)
| ❌ | Не сохраняются

| Под-книги стрим × страна
| ❌ | Лимит 5 книг на бесплатном

| Automation 360 flows
| ❌ | Лимит 5 flow; динамические сегменты — триггеры, не аудитория кампаний

| Теги SendPulse
| ❌ | `POST /campaigns` не принимает тег как фильтр ни на каком тарифе

| SendPulse Standard (10 сегментов)
| ❌ | Нужно 55+ (11 стримов × 5 стран); сегменты привязаны к книге, не глобальные

| SendPulse Pro (50 сегментов)
| ❓ | Лимит 50 — но неизвестно: на книгу или на аккаунт. Если на книгу (50/книга × 11 книг) — хватает. Если на аккаунт — нет. Уточнить у поддержки: _"Is the saved segment limit per address book or per account?"_

| SendPulse Enterprise
| ⚠️ | Безлимит, но дорого

| *Unisender* (аккаунт пока не куплен)
| ✅ | Теги программно через API, без UI, без лимитов на количество — технически лучший вариант из рассмотренных
|===

*Развилка остаётся открытой.* Unisender — предпочтительный вариант по результатам исследования, но аккаунт не куплен и переход не подтверждён — нужно бизнес-решение и бюджет на подписку. Без этого решения TASK-25 не начинать; см. <<TASK-26: Нейтральная формулировка вместо счётчика кандидатов в письме>> — временное закрытие разрыва на этапе MVP, не требующее этого решения.

*Архитектура (Unisender):*

При подтверждении работодателя — присваивать тег на каждую комбинацию стрим × страна:

----
kz_finance, kz_marketing, ...    ← если country = Казахстан
uz_finance, uz_marketing, ...    ← если country = Узбекистан
any_finance, any_marketing, ...  ← если country = Любая
----

При рассылке: `createEmailMessage(list_id, tag="kz_finance")` + `createCampaign`. Теги создаются программно — вручную в UI ничего не нужно.

*Что нужно сделать при реализации:*

. Создать `lib/unisender.ts` — клиент на API-ключе (OAuth не нужен). Методы: `subscribe`, `importContacts`, `createList`, `createEmailMessage`, `createCampaign`.
. `confirmEmployer()` — добавлять в список Unisender вместо SendPulse; присваивать теги по стримам × стране.
. `publishMailingList()` — `createEmailMessage` с `tag="{country}_{stream_slug}"` + `createCampaign`.
. Env: `UNISENDER_API_KEY` вместо `SENDPULSE_*`.

---

=== 🟠 Важно — автоматизация подборок

NOTE: ℹ️ Теги кандидата (`level`, `industry`, `func`, `countryPrimary`, `countryDesired` — TASK-04 ✅) уже есть в данных. Маппинг тег→стрим реализован (TASK-27 ✅, см. <<Выполнено>>) — но выпуски всё ещё собираются полностью вручную в листе «Mailing lists». Задачи ниже — оставшиеся шаги к автоподбору.

==== TASK-28: Автоподбор кандидатов в выпуск

*Что сейчас:* нет никакой автоматики — редактор вручную прописывает `list_id`/`candidate_id`/`stream`/`date` строки в листе «Mailing lists».

*Что нужно сделать:*

* На основе `getCandidatesForStream()` (TASK-27 ✅, маппинг тег→стрим) и TASK-05 (правила публикации — статус/пауза) — сформировать функцию, которая для выбранного стрима возвращает кандидатов, готовых к публикации: статус «Активный», не в паузе (TASK-05), с совпадением тега.
* Явно исключить кандидатов, уже включённых в предыдущий выпуск этого же стрима без прошедшей паузы (правило «не публиковать чаще раза в 2 недели» из TASK-05, §13 ТЗ).
* Результат — предложение (draft), не автопубликация: редактор должен посмотреть и подтвердить список перед созданием выпуска (человек в контуре — как сейчас с подтверждением работодателей/кандидатов).
* Использовать существующий порог из TASK-21 (6–10 кандидатов) как сигнал: если авто-подбор даёт меньше 6 — предупреждать редактора до создания выпуска, а не после.

*Сложность:* средняя. *Зависит от:* TASK-05 (TASK-27 уже выполнена).

---

==== TASK-29: Конструктор выпуска в редакторе (UI)

*Что сейчас:* выпуски существуют только как строки в листе «Mailing lists» Google Sheets; в редакторе — только просмотр (`/editor` — список выпусков + кнопка «Отправить»), создания/редактирования нет.

*Что нужно сделать:*

* Новый раздел в редакторе (`/editor/releases/new` или расширение `/editor`): выбор стрима → авто-подбор кандидатов (TASK-28) как предзаполненный список с чекбоксами → редактор убирает/добавляет кандидатов вручную → сохранение создаёт выпуск.
* Хранить выпуски в Neon (сближает с TASK-DB-5 из плана перехода на СУБД — тот же шаг, но с фокусом на автоматизацию, а не только миграцию данных) — таблицы `mailing_lists` + `mailing_list_candidates` по схеме, уже описанной в разделе «Переход на реляционную СУБД».
* После сохранения — тот же экран публикации, что сейчас (`PublishButton`), без изменений в `publishMailingList`, кроме источника данных (Neon вместо листа).

*Сложность:* высокая — новый UI + перенос источника данных выпусков. *Зависит от:* TASK-28.

*Связано с:* TASK-DB-5 (отложенная задача переноса Mailing Lists в Neon) — эта задача закрывает её как побочный эффект.

---

=== 🟠 Важно — бизнес-правила

==== TASK-05: Правила публикации кандидатов

*Что сейчас:* нет отслеживания дат публикации, нет защиты от нарушения правил.

*По ТЗ (§13):*

* Первая публикация → фиксируется дата.
* Через 2 недели возможна повторная публикация.
* После второй публикации — пауза 3 месяца.
* Кандидат может попросить статус «Не публиковать».

*Что нужно сделать:*

* Добавить в карточку кандидата (Google Sheets): дата первой публикации, дата последней публикации, статус (Активный / На паузе до даты / Не публиковать).
* В редакторе при выборе кандидатов для выпуска: показывать статус публикации и дату, с которой публикация снова разрешена.
* Предупреждение, если кандидат добавлен в подборку, но находится в паузе.

---

=== 🟡 Нужно для полноты MVP

==== TASK-12: Summary кандидата в теле email-рассылки

*Что сейчас:* письмо содержит только ссылку на страницу подборки.

*По ТЗ (§10):* в анонсе после вступительного текста показывается *первое Summary* кандидата из выпуска, чтобы дать работодателю предпросмотр до перехода по ссылке.

*Что нужно сделать:*

* В `publishMailingList` (`app/actions.ts`): получить первого кандидата из выпуска.
* Добавить его анонимное Summary в HTML тела письма перед кнопкой «Смотреть всех кандидатов».

---

==== TASK-14: Уведомление работодателя о результате запроса на контакт

*Решение принято:* ручная коммуникация менеджера. Автоматическая отправка через SendPulse — возможная доработка после MVP, см. <<Вне MVP (задокументировано, реализовать позже)>>.

---

=== 🟢 Идея — комментарии к кандидатам

==== TASK-34: Комментарии к кандидатам в редакторе

*Контекст:* до этого внутренние заметки по кандидату жили как три свободных текстовых колонки прямо в Google Sheets (`Интервьюер`/`Баллы`/`Примечания`) — незаполненные, без структуры, без истории, удалены. Взамен предложена нормальная функциональность: отдельная таблица комментариев в Neon, по аналогии с `candidateResumes` (TASK-32) — история версий вместо одной ячейки, которую последний редактор перезаписывает поверх предыдущей.

*Схема (предложение):*
[source,ts]
----
export const candidateComments = pgTable("candidateComments", {
  id:          text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  candidateId: text("candidateId").notNull(),
  timestamp:   timestamp("timestamp").defaultNow().notNull(),
  text:        text("text").notNull(),
})
----

`candidateId` — не FK (кандидаты остаются в Sheets, как и `candidateResumes.candidateId` сегодня).

*Открытый вопрос — индексы:* `candidateId` почти наверняка нужен (единственный паттерн доступа — «все комментарии одного кандидата», тот же профиль обращения, что уже есть у `candidateResumes`, где индекса нет, но и объём на кандидата исторически маленький). Сортировка по `timestamp` — открытый вопрос: индекс в БД (`ORDER BY timestamp DESC` со стороны Postgres) против сортировки на уровне приложения после выборки по `candidateId` (как уже сделано для истории резюме — `getResumeVersions()` сортирует в SQL, но без отдельного индекса, и на текущих объёмах разницы не видно). Решить на реализации, ориентируясь на ожидаемое число комментариев на кандидата — если счёт идёт на десятки, а не тысячи, отдельный индекс по `timestamp`, скорее всего, не нужен.

*Что нужно сделать (грубый план, не детализировано):*

* `lib/db/schema.ts` — таблица выше, миграция через `db:generate`/`db:migrate`.
* `lib/db/candidate-comments.ts` — `addComment(candidateId, text)`, `getComments(candidateId)`.
* `app/actions.ts` — Server Actions, `revalidatePath("/editor/candidates")`.
* UI — скорее всего в `CandidateEditModal`, по аналогии с блоком истории резюме (TASK-32): список с датами, форма добавления. Открытый вопрос — нужно ли редактирование/удаление комментария или только добавление (append-only лог, как у истории резюме).

*Приоритет:* не назначен — задача зафиксирована по запросу, дальше в очередь не ставилась.

---

=== 🔵 Опционально — Vercel Global Config для стримов

NOTE: ℹ️ Стримы хранятся в Neon (TASK-DB-8 ✅). Global Config рассматривается как опциональная оптимизация: чтение из CDN-ноды за < 1 мс против ~50 мс запроса к Neon. UI управления стримами уже готов (`components/streams-table.tsx`) — задачи ниже касаются только смены бэкенда хранения, не UI.

Vercel Global Config (Edge Config) — глобально реплицированное key-value хранилище, читается прямо на CDN-ноде без обращения к серверу. Лимиты: 1 МБ, 100 000 чтений/мес, 100 записей/мес, задержка записи до 10 секунд.

==== TASK-GC-1: Инфраструктура и первичное наполнение

. Создать Edge Config store в Vercel Dashboard (Storage → Edge Config).
. Установить `@vercel/edge-config`.
. Добавить переменные окружения:
* `EDGE_CONFIG` — connection string (проставляется автоматически при подключении через Marketplace)
* `VERCEL_API_TOKEN` — токен с правом на запись в Edge Config (создать в Vercel Account Settings → Tokens)
* `EDGE_CONFIG_ID` — ID стора (вида `ecfg_...`, виден в дашборде)
. Перенести начальные данные из Neon в Edge Config через Dashboard или API: `{ "streams": [{ "id": 1, "name": "Finance", "type": "Industry", "description": "…" }, …] }`.

*Сложность:* низкая.

---

==== TASK-GC-2: Замена getStreams() / getStreamsDetailed() на Edge Config

Заменить реализацию в `lib/db/streams.ts` — сигнатуры и все вызовы остаются прежними.

[source,ts]
----
import { get } from "@vercel/edge-config"

export async function getStreams(): Promise<string[]> {
  const streams = await get<StreamRecord[]>("streams")
  return (streams ?? []).map((s) => s.name)
}

export async function getStreamsDetailed(): Promise<StreamRecord[]> {
  return (await get<StreamRecord[]>("streams")) ?? []
}
----

Функции записи (`createStreamRecord`, `updateStreamRecord`, `deleteStreamRecord`) переключить на Edge Config API вместо Drizzle. Server Actions в `app/actions.ts` остаются без изменений.

*Сложность:* низкая.

---

==== TASK-GC-3: Адаптация Server Actions для записи в Edge Config

NOTE: ℹ️ UI управления стримами уже реализован в TASK-DB-8 (`components/streams-table.tsx`, Server Actions `createStream/updateStream/deleteStream`). TASK-GC-3 — только смена бэкенда записи с Drizzle/Neon на Edge Config API.

Заменить реализацию Server Actions в `app/actions.ts`:

[source,ts]
----
// Вспомогательная функция записи в Edge Config
async function updateStreamsInConfig(streams: StreamRecord[]): Promise<void> {
  await fetch(`https://api.vercel.com/v1/edge-config/${process.env.EDGE_CONFIG_ID}/items`, {
    method: "PATCH",
    headers: {
      Authorization: `Bearer ${process.env.VERCEL_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ items: [{ operation: "upsert", key: "streams", value: streams }] }),
  })
}
----

Все три action-а (`createStream`, `updateStream`, `deleteStream`) читают текущий массив из Edge Config, вносят изменение и пишут обратно через `updateStreamsInConfig`. `revalidatePath` остаётся без изменений.

*Сложность:* низкая — только замена слоя хранения, UI не меняется.

NOTE: ℹ️ Задержка записи в Global Config — до 10 секунд до глобального распространения. Для управления стримами это несущественно.

---

=== Отложено — миграция оставшихся таблиц в Neon

NOTE: ℹ️ Полный анализ, стоимость и схема данных — в разделе <<Приложение: переход на реляционную СУБД>>. Ниже — только статус по оставшимся таблицам.

==== TASK-DB-4 (отложено): Candidates → Neon

Основной блокер снят почти полностью: `CandidateEditModal` (TASK-23 ✅, расширена дважды) теперь редактирует контакты, `title`, `summary`, `level`, `stream`, `countryPrimary`/`countryDesired`, `activeSince` — включая оба поля, которые видит работодатель на карточке подборки. Не редактируются через UI: `industry`, `func`, `excludedCompanies`/`excludedIndustries` — эти поля (в основном неиспользуемые системой или редко нужные) редактор по-прежнему заполняет напрямую в Sheets. `bio`/`website`/`location` убраны из кода целиком (см. ниже) — колонок с такими названиями в реальной таблице никогда не было. Миграция хранения в Neon сама по себе ещё не сделана.

---

==== TASK-DB-5 (отложено): Mailing Lists → Neon

Требует построения конструктора выпусков в редакторе (см. TASK-29 выше). Сейчас выпуски создаются вручную в листе «Mailing lists» — это основной редакторский инструмент.

---

==== TASK-DB-7 (отложено): Полный вывод Google Sheets из эксплуатации

Финальный шаг после переноса всех таблиц. Удаление `lib/sheets.ts`, отзыв прав Service Account, удаление переменных окружения.

=== ⚪ Вне MVP (задокументировано, реализовать позже)

[cols="1,3", options="header"]
|===
| Задача | Примечание

| TASK-17: SLA и эскалация запросов
| Визуальная подсветка дедлайнов в редакторе запросов (4 рабочих дня). Отложено — достаточно ручного контроля на этапе MVP.

| TASK-18: Дата уточнения готовности кандидата
| Напоминание через 3 месяца после публикации. Зависит от TASK-05 (правила публикации). Отложено на после MVP.

| TASK-06: два явных канала доставки для работодателя
| Email и телефон уже обязательны в форме регистрации — фактически два канала у работодателя всегда есть (email + телефон/WhatsApp). Отдельное поле «резервного канала» с явным выбором пользователем избыточно на данном этапе. Необходимость и форму контроля стоит обдумать после MVP.

| TASK-14 (автоматизация)
| Автоматическое уведомление работодателя о результате запроса через SendPulse. Сейчас — ручная коммуникация менеджера.

| TASK-20: Письмо кандидату со ссылкой на анкету
| Дополнительный шаг вместо прямой формы не даёт реальных преимуществ при ручной модерации. Текущая прямая форма достаточна для MVP.

| Telegram-рассылка
| По ТЗ «обязательно, но можно вручную» — приемлемо для MVP

| WhatsApp-рассылка
| По ТЗ через бизнес-аккаунт — требует отдельной интеграции

| Биллинг и управление подпиской
| Явно исключено из MVP в ТЗ

| Аналитика
| Явно исключено из MVP в ТЗ

| TASK-SEC-1: Аутентификация для `/api/resume`
| Роут отдаёт файлы резюме без проверки прав — защита только через неугадываемость UUID в URL. Добавить проверку `EDITOR_SECRET` как query-параметр: `?secret=<EDITOR_SECRET>`. Ссылки в Sheets и редакторе обновить соответственно. Актуально когда появятся реальные данные или возрастёт требование к безопасности.
|===

== Приоритизация

[cols="1,1,1,2", options="header"]
|===
| ID | Приоритет | Сложность | Примечание

| TASK-25 | 🔴 Критично, но заблокирована | Средняя-высокая | Гео-сегментация рассылки — исследование SendPulse завершено (все тарифы исчерпаны), Unisender технически подходит, но аккаунт не куплен — решение открыто
| TASK-05 | 🟠 Важно | Средняя | Зависит от TASK-04 ✅; правила публикации кандидатов; блокирует TASK-28
| TASK-28 | 🟠 Важно | Средняя | Автоподбор кандидатов в выпуск. Зависит от TASK-05 (TASK-27 ✅ уже выполнена)
| TASK-12 | 🟡 MVP | Средняя | Summary кандидата в теле email-рассылки
| TASK-29 | 🟡 После автоподбора | Высокая | Конструктор выпуска в редакторе (UI). Зависит от TASK-28; закрывает TASK-DB-5
| TASK-06 | ⚪ После MVP | Низкая | Email и телефон уже обязательны → фактически два канала всегда есть
| TASK-20 | ⚪ После MVP | — | Лишний шаг без реальных преимуществ на MVP-масштабе
|===

Полный список выполненных задач — в разделе <<Выполнено>>.

== Рекомендуемая очерёдность

NOTE: ℹ️ TASK-25 требует решения о переходе на Unisender (см. разбор задачи выше) — аккаунт не куплен, развилка открыта, это вопрос бизнес-приоритета и бюджета, а не инженерная блокировка. До этого решения TASK-25 не начинать. Связанный разрыв доверия («в письме указано больше кандидатов, чем видно на странице») уже закрыт отдельно — TASK-26 ✅, см. <<Выполнено>>.

. *TASK-05* — правила публикации кандидатов. Без него автоподбор (TASK-28) не может учитывать паузы. TASK-27 (маппинг тег→стрим) уже выполнена и его не блокирует.
. *TASK-28* — автоподбор кандидатов в выпуск. Основной эффект автоматизации подборок.
. *TASK-12* — Summary в теле письма.
. *TASK-29* — конструктор выпуска в редакторе (UI). Наиболее объёмная задача — переносить, когда автоподбор (TASK-28) уже проверен на практике вручную.
. *TASK-25* — гео-сегментация рассылки. Не в очереди инженерных задач — ждёт бизнес-решения о переходе на Unisender и покупки аккаунта. Как только решение принято, встраивать в план вне зависимости от прогресса по остальным пунктам (технически независима).

== Приложение: переход на реляционную СУБД

=== Анализ

Google Sheets достаточен для MVP, но создаёт структурные ограничения по мере роста:

* *Производительность:* каждый запрос — HTTP-вызов к Sheets API; нет индексов, фильтрация только in-memory. При 500+ кандидатах и 100+ работодателях это станет ощутимым.
* *Целостность данных:* нет foreign keys и транзакций — связи между листами поддерживаются вручную (candidateId, employerToken — строки без ограничений).
* *Сложные запросы:* фильтрация, JOIN-подобные операции (подборка → кандидаты → работодатели) делаются in-memory в `lib/sheets.ts`, что увеличивает объём данных, которые нужно держать в памяти.
* *Параллельные записи:* append-only подход с `rowIndex` — хрупкий при одновременных операциях.
* *Лимиты API:* Google Sheets API — 300 запросов в минуту на проект.

=== Стоимость и выбор хранилища

==== Контекст: Vercel Postgres закрыт

Vercel Postgres (декабрь 2024) был упразднён и перенесён на Neon напрямую. Сейчас Neon доступен через Vercel Marketplace — подключается из того же дашборда, переменные окружения проставляются автоматически. Это меняет картину по сравнению со сторонними сервисами.

==== Стек для Vercel-проекта: Neon + Vercel Blob

Поскольку проект хостится на Vercel, естественный выбор — держать все сервисы в одной экосистеме.

*База данных — Neon (через Vercel Marketplace):*

[cols="1,1,3", options="header"]
|===
| Tier | Цена | Что включено

| Free
| $0/мес
| 0.5 ГБ хранилища, 100 compute-unit-hours/мес (~400 ч на минимальном инстансе). Compute засыпает при простое — *без принудительной паузы проекта*. Технически пригодно для продакшена с низким трафиком.

| Launch
| $19/мес
| Usage-based ($0.106/CU-час). Без лимитов на время работы. Подходит при ежедневном трафике.
|===

*Файловое хранилище — Vercel Blob:*

[cols="1,1,3", options="header"]
|===
| Tier | Цена | Что включено

| Hobby (с Vercel Hobby)
| $0/мес
| 1 ГБ хранилища, 10 ГБ transfer, 10 000 read-операций, 2 000 write-операций. При превышении — блокировка до конца расчётного периода (без доплаты).

| Pro (с Vercel Pro)
| Входит в $20/мес Vercel Pro
| 5 ГБ хранилища, 100 ГБ transfer, 100 000 read-операций, 10 000 write-операций. Сверх лимитов — $0.023/ГБ.
|===

==== Vercel Global Config (бывший Edge Config) — не БД

Отдельный продукт Vercel: глобально реплицированное key-value хранилище, читается прямо на CDN-ноде в регионе пользователя — без обращения к серверу. Чтение < 1 мс, P99 ≈ 15 мс.

*Лимиты:* 1 МБ на стор, 100 000 чтений/мес (бесплатно), 100 записей/мес (бесплатно), распространение записи — до 10 секунд по всем регионам.

*Когда подходит:* данные читаются постоянно, записываются редко — feature flags, списки стран/тегов, конфигурация. Не подходит для транзакционных данных, фильтрации, запросов — это не база данных.

*Применимость для TalentStreams:* список стримов (`getStreams()`) — идеальный кандидат, см. <<🔵 Опционально — Vercel Global Config для стримов>>.

==== Итоговые сценарии для TalentStreams

[cols="2,1,1,1", options="header"]
|===
| Критерий | Neon Free + Blob Hobby | Neon Launch + Blob Pro | Supabase Pro (отдельно)

| Стоимость/мес (доп. к Vercel)
| *$0*
| *$19* (+ $20 Vercel Pro)
| *$25* (+ Vercel)

| Приостановка БД в продакшене
| ✅ Нет (sleep, не пауза)
| ✅ Нет
| ⛔ Free: да. Pro: нет.

| Файловое хранилище (резюме)
| ✅ Vercel Blob
| ✅ Vercel Blob
| ✅ Supabase Storage

| Интеграция с Vercel
| ✅ Нативная (Marketplace)
| ✅ Нативная (Marketplace)
| ⚠ Сторонний сервис

| Один дашборд / один счёт
| ✅ Да
| ✅ Да
| ❌ Нет

| Риски при текущем масштабе
| ⚠ 400 ч/мес (риск при росте)
| ✅ Нет
| ✅ Нет
|===

=== Рекомендация

*На этапе разработки и раннего MVP* — Neon Free + Vercel Blob Hobby. Никаких дополнительных расходов, нет паузы проекта, резюме хранятся в Vercel Blob (1 ГБ хватит на сотни файлов).

*При регулярном ежедневном трафике* (появились реальные клиенты) — апгрейд Neon до Launch ($19/мес). Vercel Blob Hobby, скорее всего, продолжит покрывать потребности.

*Против Supabase:* при хостинге на Vercel Supabase становится лишним сторонним сервисом — отдельный аккаунт, отдельный счёт, ручная настройка env vars. Преимуществ перед Neon+Blob нет: оба дают PostgreSQL + файловое хранилище, но Neon нативно интегрирован в Vercel.

*Технический выигрыш от перехода:* `lib/sheets.ts` (1039 строк, весь header-mapping и `ensureColumns`-код) заменяется на `lib/db/` с Drizzle — схема на TypeScript, SQL-like запросы, минимальный бандл без cold-start штрафа на Vercel. TASK-DB-5 (резюме) реализуется через `@vercel/blob`.

=== Схема данных (PostgreSQL)

[source,sql]
----
candidates       -- профили + регистрации (текущий главный лист)
  id UUID PK, name, title, email, phone,
  stream, level, industry, func, country_primary, country_desired,
  summary, excluded_companies TEXT[], excluded_industries TEXT[],
  status, active_since, registration_timestamp, cover_letter, resume_url

employers        -- работодатели
  id UUID PK, timestamp, name, company, email, phone,
  telegram, linkedin, primary_contact, streams TEXT[],
  country, additional_countries TEXT[], status

streams          -- список стримов
  id SERIAL PK, name TEXT UNIQUE, type TEXT NOT NULL DEFAULT '', description TEXT NOT NULL DEFAULT ''

mailing_lists    -- выпуски
  id UUID PK, stream, date
mailing_list_candidates   -- связь выпуск ↔ кандидат
  list_id UUID FK, candidate_id UUID FK

contact_requests -- запросы на контакт
  id UUID PK, timestamp, list_id UUID FK, stream,
  candidate_id UUID FK (nullable — общий запрос),
  employer_id UUID FK, employer_name, company, employer_email, status
----

NOTE: ℹ️ Это первоначальный проектный набросок (до реализации `employers`/`contact_requests`), а не текущая схема — по факту `employers.id` стал `employers.token`, а `contact_requests` после переноса `employers` в Neon был нормализован (`employer_name`/`company`/`employer_email` убраны как дубли, `stream` заменён на `stream_id` FK) — см. реальный `lib/db/schema.ts` и <<✅ Нормализация contactRequests: FK на employers и streams вместо дублей>>.

=== Стратегия: постепенный переход

Таблицы делятся по тому, кто в них пишет:

[cols="2,1,1,1", options="header"]
|===
| Таблица | Пишет приложение | Правит редактор в Sheets | Статус

| Contact Requests | ✅ Только оно | ❌ Никогда | ✅ Перенесено в Neon (TASK-DB-2)
| Streams | ❌ | ✅ | ✅ Перенесено в Neon + UI готов (TASK-DB-8)
| Employers | ✅ Полностью через UI | ❌ Никогда (лист остаётся архивом) | ✅ Перенесено в Neon (TASK-DB-3)
| Candidates | ✅ Регистрация + `CandidateEditModal` (TASK-23, расширена) | ⚠ Только поля вне формы редактирования (industry/func/excluded*) | Остаётся в Sheets — TASK-DB-4, отложено (частичный редактор в UI уже есть, миграция хранения — нет)
| Mailing Lists | ❌ | ✅ Полностью | Остаётся в Sheets — TASK-DB-5, отложено (нужен конструктор выпусков в UI)
|===

Первый шаг — Contact Requests и Streams — уже сделаны: это единственные таблицы, которые редактор никогда (Contact Requests) или крайне редко и только через UI (Streams) не трогает напрямую в Sheets. Остальные таблицы остаются в Sheets до появления соответствующего UI в приложении — статус и детали см. <<Отложено — миграция оставшихся таблиц в Neon>>.

NOTE: ℹ️ `lib/sendpulse.ts` миграции не требует. Схема Drizzle расширяется по мере переноса таблиц — новые таблицы добавляются в `lib/db/schema.ts` (`employers` добавлена в TASK-DB-3 ✅; ещё предстоят TASK-DB-4/5).
